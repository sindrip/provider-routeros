version: "2"

linters:
  enable:
    # Codified style: logical statement groups separated by blank lines.
    - wsl_v5
    - whitespace
  settings:
    errcheck:
      exclude-functions:
        - (io.ReadCloser).Close
        - (net/http.ResponseWriter).Write

formatters:
  enable:
    - gofmt
