version: '3'

tasks:
  probe:
    desc: Boot the lab, pin observations, tear down
    cmd: go run ./cmd/probe

  test:
    desc: Unit tests, no docker
    cmd: go test ./...

  lint:
    desc: golangci-lint over the workspace
    cmd: go tool golangci-lint run
