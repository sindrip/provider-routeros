// SPDX-FileCopyrightText: 2024 The Crossplane Authors <https://crossplane.io>
//
// SPDX-License-Identifier: Apache-2.0

// Code generated by upjet. DO NOT EDIT.

package v1alpha1

import (
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/runtime/schema"

	v2 "github.com/crossplane/crossplane/apis/v2/core/v2"
)

type ItemsInitParameters struct {

	// (String) Resource name in the notation ```routeros_ip_firewall_filter```.
	// Resource name in the notation ```routeros_ip_firewall_filter```.
	ResourceName *string `json:"resourceName,omitempty" tf:"resource_name,omitempty"`

	// (String) URL path of the resource in the notation ```/ip/firewall/filter```.
	// URL path of the resource in the notation ```/ip/firewall/filter```.
	ResourcePath *string `json:"resourcePath,omitempty" tf:"resource_path,omitempty"`

	// (List of String) List identifiers in the required sequence. To locate the ```sequence``` before an existing rule, add its ```id``` to the last element of the ```sequence```.
	// Rows of the menu in the desired order, each identified by its comment (an entry starting with `*` is taken as a literal RouterOS id instead). The last entry is the anchor: it is never moved, and all preceding entries are placed before it in the listed order. Every comment must resolve to exactly one existing row before the sequence can be applied; rows not listed are left where they are.
	Sequence []*string `json:"sequence,omitempty" tf:"sequence,omitempty"`
}

type ItemsObservation struct {

	// (String) The ID of this resource.
	ID *string `json:"id,omitempty" tf:"id,omitempty"`

	// (String) Resource name in the notation ```routeros_ip_firewall_filter```.
	// Resource name in the notation ```routeros_ip_firewall_filter```.
	ResourceName *string `json:"resourceName,omitempty" tf:"resource_name,omitempty"`

	// (String) URL path of the resource in the notation ```/ip/firewall/filter```.
	// URL path of the resource in the notation ```/ip/firewall/filter```.
	ResourcePath *string `json:"resourcePath,omitempty" tf:"resource_path,omitempty"`

	// (List of String) List identifiers in the required sequence. To locate the ```sequence``` before an existing rule, add its ```id``` to the last element of the ```sequence```.
	// Rows of the menu in the desired order, each identified by its comment (an entry starting with `*` is taken as a literal RouterOS id instead). The last entry is the anchor: it is never moved, and all preceding entries are placed before it in the listed order. Every comment must resolve to exactly one existing row before the sequence can be applied; rows not listed are left where they are.
	Sequence []*string `json:"sequence,omitempty" tf:"sequence,omitempty"`
}

type ItemsParameters struct {

	// (String) Resource name in the notation ```routeros_ip_firewall_filter```.
	// Resource name in the notation ```routeros_ip_firewall_filter```.
	// +kubebuilder:validation:Optional
	ResourceName *string `json:"resourceName,omitempty" tf:"resource_name,omitempty"`

	// (String) URL path of the resource in the notation ```/ip/firewall/filter```.
	// URL path of the resource in the notation ```/ip/firewall/filter```.
	// +kubebuilder:validation:Optional
	ResourcePath *string `json:"resourcePath,omitempty" tf:"resource_path,omitempty"`

	// (List of String) List identifiers in the required sequence. To locate the ```sequence``` before an existing rule, add its ```id``` to the last element of the ```sequence```.
	// Rows of the menu in the desired order, each identified by its comment (an entry starting with `*` is taken as a literal RouterOS id instead). The last entry is the anchor: it is never moved, and all preceding entries are placed before it in the listed order. Every comment must resolve to exactly one existing row before the sequence can be applied; rows not listed are left where they are.
	// +kubebuilder:validation:Optional
	Sequence []*string `json:"sequence,omitempty" tf:"sequence,omitempty"`
}

// ItemsSpec defines the desired state of Items
type ItemsSpec struct {
	v2.ManagedResourceSpec `json:",inline"`
	ForProvider            ItemsParameters `json:"forProvider"`
	// THIS IS A BETA FIELD. It will be honored
	// unless the Management Policies feature flag is disabled.
	// InitProvider holds the same fields as ForProvider, with the exception
	// of Identifier and other resource reference fields. The fields that are
	// in InitProvider are merged into ForProvider when the resource is created.
	// The same fields are also added to the terraform ignore_changes hook, to
	// avoid updating them after creation. This is useful for fields that are
	// required on creation, but we do not desire to update them after creation,
	// for example because of an external controller is managing them, like an
	// autoscaler.
	InitProvider ItemsInitParameters `json:"initProvider,omitempty"`
}

// ItemsStatus defines the observed state of Items.
type ItemsStatus struct {
	v2.ManagedResourceStatus `json:",inline"`
	AtProvider               ItemsObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:storageversion

// Items is the Schema for the Itemss API.
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"
// +kubebuilder:resource:scope=Namespaced,categories={crossplane,managed,routeros}
type Items struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`
	// +kubebuilder:validation:XValidation:rule="!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.sequence) || (has(self.initProvider) && has(self.initProvider.sequence))",message="spec.forProvider.sequence is a required parameter"
	Spec   ItemsSpec   `json:"spec"`
	Status ItemsStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ItemsList contains a list of Itemss
type ItemsList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []Items `json:"items"`
}

// Repository type metadata.
var (
	Items_Kind             = "Items"
	Items_GroupKind        = schema.GroupKind{Group: CRDGroup, Kind: Items_Kind}.String()
	Items_KindAPIVersion   = Items_Kind + "." + CRDGroupVersion.String()
	Items_GroupVersionKind = CRDGroupVersion.WithKind(Items_Kind)
)

func init() {
	SchemeBuilder.Register(&Items{}, &ItemsList{})
}
