---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ports.bridge.routeros.m.sindrip.io
spec:
  group: bridge.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: Port
    listKind: PortList
    plural: ports
    singular: port
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Port is the Schema for the Ports API. #### This is an alias
          for backwards compatibility between plugin versions. Please see documentation
          for [routeros_interface_bridge_port](interface_bridge_port.md)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PortSpec defines the desired state of Port
            properties:
              forProvider:
                properties:
                  autoIsolate:
                    description: When enabled, prevents a port moving from discarding
                      into forwarding state if no BPDUs are received from the neighboring
                      bridge. The port will change into a forwarding state only when
                      a BPDU is received. This property only has an effect when protocol-mode
                      is set to rstp or mstp and edge is set to no.
                    type: boolean
                  bpduGuard:
                    description: This property has no effect when protocol-mode is
                      set to none.
                    type: boolean
                  bridge:
                    type: string
                  broadcastFlood:
                    description: When enabled, bridge floods broadcast traffic to
                      all bridge egress ports. When disabled, drops broadcast traffic
                      on egress ports.
                    type: boolean
                  comment:
                    type: string
                  disabled:
                    type: boolean
                  edge:
                    description: Set port as edge port or non-edge port, or enable
                      edge discovery. Edge ports are connected to a LAN that has no
                      other bridges attached.
                    type: string
                  fastLeave:
                    description: Enables IGMP Fast leave feature on the port.
                    type: boolean
                  frameTypes:
                    description: Specifies allowed ingress frame types on a bridge
                      port. This property only has effect when vlan-filtering is set
                      to yes.
                    type: string
                  horizon:
                    description: Use split horizon bridging to prevent bridging loops.
                      Set the same value for group of ports, to prevent them from
                      sending data to ports with the same horizon value. Split horizon
                      is a software feature that disables hardware offloading. This
                      value is integer '0'..'429496729' or 'none'.
                    type: string
                  hw:
                    description: Enable or disable Hardware Offloading of the interface.
                    type: boolean
                  ingressFiltering:
                    description: Enables or disables VLAN ingress filtering, which
                      checks if the ingress port is a member of the received VLAN
                      ID in the bridge VLAN table. Should be used with frame-types
                      to specify if the ingress traffic should be tagged or untagged.
                      This property only has effect when vlan-filtering is set to
                      yes.
                    type: boolean
                  interface:
                    description: Name of the interface.
                    type: string
                  internalPathCost:
                    description: Path cost to the interface for MSTI0 inside a region.
                      This property only has effect when protocol-mode is set to mstp.
                    format: int64
                    type: integer
                  learn:
                    description: Changes MAC learning behaviour on a bridge port
                    type: string
                  multicastRouter:
                    description: Changes the state of a bridge port whether IGMP membership
                      reports are going to be forwarded to this port.
                    type: string
                  mvrpApplicantState:
                    description: 'MVRP applicant options (available since RouterOS
                      7.15): - non-participant - port does not send any MRP messages;
                      - normal-participant - port participates normally in MRP exchanges.'
                    type: string
                  mvrpRegistrarState:
                    description: 'MVRP registrar options (available since RouterOS
                      7.15): - fixed - port ignores all MRP messages, and remains
                      Registered (IN) in all configured vlans. - normal - port receives
                      MRP messages and handles them according to the standard.'
                    type: string
                  pathCost:
                    description: Path cost to the interface, used by STP to determine
                      the "best" path, used by MSTP todetermine "best" path between
                      regions. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  pointToPoint:
                    description: Specifies if a bridge port is connected to a bridge
                      using a point-to-point link for faster convergence in case of
                      failure. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  priority:
                    description: The priority of the interface, used by STP to determine
                      the root port, used by MSTP to determine root port between regions.
                    type: string
                  pvid:
                    description: ort VLAN ID (pvid) specifies which VLAN the untagged
                      ingress traffic is assigned to. This property only has effect
                      when vlan-filtering is set to yes.
                    format: int64
                    type: integer
                  restrictedRole:
                    description: Enable the restricted role on a port, used by STP
                      to forbid a port becoming a root port. This property only has
                      effect when protocol-mode is set to mstp.
                    type: boolean
                  restrictedTcn:
                    description: Disable topology change notification (TCN) sending
                      on a port, used by STP to forbid network topology changes to
                      propagate. This property only has effect when protocol-mode
                      is set to mstp.
                    type: boolean
                  tagStacking:
                    description: Forces all packets to be treated as untagged packets.
                      Packets on ingress port will be tagged with another VLAN tag
                      regardless if a VLAN tag already exists, packets will be tagged
                      with a VLAN ID that matches the pvid value and will use EtherType
                      that is specified in ether-type. This property only has effect
                      when vlan-filtering is set to yes.
                    type: boolean
                  trusted:
                    description: When enabled, it allows to forward DHCP packets towards
                      DHCP server through this port. Mainly used to limit unauthorized
                      servers to provide malicious information for users. This property
                      only has effect when dhcp-snooping is set to yes.
                    type: boolean
                  unknownMulticastFlood:
                    description: When enabled, bridge floods unknown multicast traffic
                      to all bridge egress ports.
                    type: boolean
                  unknownUnicastFlood:
                    description: When enabled, bridge floods unknown unicast traffic
                      to all bridge egress ports.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoIsolate:
                    description: When enabled, prevents a port moving from discarding
                      into forwarding state if no BPDUs are received from the neighboring
                      bridge. The port will change into a forwarding state only when
                      a BPDU is received. This property only has an effect when protocol-mode
                      is set to rstp or mstp and edge is set to no.
                    type: boolean
                  bpduGuard:
                    description: This property has no effect when protocol-mode is
                      set to none.
                    type: boolean
                  bridge:
                    type: string
                  broadcastFlood:
                    description: When enabled, bridge floods broadcast traffic to
                      all bridge egress ports. When disabled, drops broadcast traffic
                      on egress ports.
                    type: boolean
                  comment:
                    type: string
                  disabled:
                    type: boolean
                  edge:
                    description: Set port as edge port or non-edge port, or enable
                      edge discovery. Edge ports are connected to a LAN that has no
                      other bridges attached.
                    type: string
                  fastLeave:
                    description: Enables IGMP Fast leave feature on the port.
                    type: boolean
                  frameTypes:
                    description: Specifies allowed ingress frame types on a bridge
                      port. This property only has effect when vlan-filtering is set
                      to yes.
                    type: string
                  horizon:
                    description: Use split horizon bridging to prevent bridging loops.
                      Set the same value for group of ports, to prevent them from
                      sending data to ports with the same horizon value. Split horizon
                      is a software feature that disables hardware offloading. This
                      value is integer '0'..'429496729' or 'none'.
                    type: string
                  hw:
                    description: Enable or disable Hardware Offloading of the interface.
                    type: boolean
                  ingressFiltering:
                    description: Enables or disables VLAN ingress filtering, which
                      checks if the ingress port is a member of the received VLAN
                      ID in the bridge VLAN table. Should be used with frame-types
                      to specify if the ingress traffic should be tagged or untagged.
                      This property only has effect when vlan-filtering is set to
                      yes.
                    type: boolean
                  interface:
                    description: Name of the interface.
                    type: string
                  internalPathCost:
                    description: Path cost to the interface for MSTI0 inside a region.
                      This property only has effect when protocol-mode is set to mstp.
                    format: int64
                    type: integer
                  learn:
                    description: Changes MAC learning behaviour on a bridge port
                    type: string
                  multicastRouter:
                    description: Changes the state of a bridge port whether IGMP membership
                      reports are going to be forwarded to this port.
                    type: string
                  mvrpApplicantState:
                    description: 'MVRP applicant options (available since RouterOS
                      7.15): - non-participant - port does not send any MRP messages;
                      - normal-participant - port participates normally in MRP exchanges.'
                    type: string
                  mvrpRegistrarState:
                    description: 'MVRP registrar options (available since RouterOS
                      7.15): - fixed - port ignores all MRP messages, and remains
                      Registered (IN) in all configured vlans. - normal - port receives
                      MRP messages and handles them according to the standard.'
                    type: string
                  pathCost:
                    description: Path cost to the interface, used by STP to determine
                      the "best" path, used by MSTP todetermine "best" path between
                      regions. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  pointToPoint:
                    description: Specifies if a bridge port is connected to a bridge
                      using a point-to-point link for faster convergence in case of
                      failure. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  priority:
                    description: The priority of the interface, used by STP to determine
                      the root port, used by MSTP to determine root port between regions.
                    type: string
                  pvid:
                    description: ort VLAN ID (pvid) specifies which VLAN the untagged
                      ingress traffic is assigned to. This property only has effect
                      when vlan-filtering is set to yes.
                    format: int64
                    type: integer
                  restrictedRole:
                    description: Enable the restricted role on a port, used by STP
                      to forbid a port becoming a root port. This property only has
                      effect when protocol-mode is set to mstp.
                    type: boolean
                  restrictedTcn:
                    description: Disable topology change notification (TCN) sending
                      on a port, used by STP to forbid network topology changes to
                      propagate. This property only has effect when protocol-mode
                      is set to mstp.
                    type: boolean
                  tagStacking:
                    description: Forces all packets to be treated as untagged packets.
                      Packets on ingress port will be tagged with another VLAN tag
                      regardless if a VLAN tag already exists, packets will be tagged
                      with a VLAN ID that matches the pvid value and will use EtherType
                      that is specified in ether-type. This property only has effect
                      when vlan-filtering is set to yes.
                    type: boolean
                  trusted:
                    description: When enabled, it allows to forward DHCP packets towards
                      DHCP server through this port. Mainly used to limit unauthorized
                      servers to provide malicious information for users. This property
                      only has effect when dhcp-snooping is set to yes.
                    type: boolean
                  unknownMulticastFlood:
                    description: When enabled, bridge floods unknown multicast traffic
                      to all bridge egress ports.
                    type: boolean
                  unknownUnicastFlood:
                    description: When enabled, bridge floods unknown unicast traffic
                      to all bridge egress ports.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.bridge is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.bridge)
                || (has(self.initProvider) && has(self.initProvider.bridge))'
            - message: spec.forProvider.interface is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.interface)
                || (has(self.initProvider) && has(self.initProvider.interface))'
          status:
            description: PortStatus defines the observed state of Port.
            properties:
              atProvider:
                properties:
                  autoIsolate:
                    description: When enabled, prevents a port moving from discarding
                      into forwarding state if no BPDUs are received from the neighboring
                      bridge. The port will change into a forwarding state only when
                      a BPDU is received. This property only has an effect when protocol-mode
                      is set to rstp or mstp and edge is set to no.
                    type: boolean
                  bpduGuard:
                    description: This property has no effect when protocol-mode is
                      set to none.
                    type: boolean
                  bridge:
                    type: string
                  broadcastFlood:
                    description: When enabled, bridge floods broadcast traffic to
                      all bridge egress ports. When disabled, drops broadcast traffic
                      on egress ports.
                    type: boolean
                  comment:
                    type: string
                  designatedBridge:
                    description: Root bridge ID (bridge priority and the bridge MAC
                      address).
                    type: string
                  designatedBridgeId:
                    description: Shows the designated bridge identifier, as determined
                      from the port's priority vector.
                    type: string
                  designatedCost:
                    description: Designated cost.
                    type: string
                  designatedPortId:
                    description: Shows the designated port identifier, as determined
                      from the port's priority vector.
                    type: string
                  designatedPortNumber:
                    description: Designated port number.
                    format: int64
                    type: integer
                  disabled:
                    type: boolean
                  dynamic:
                    description: Configuration item created by software, not by management
                      interface. It is not exported, and cannot be directly modified.
                    type: boolean
                  edge:
                    description: Set port as edge port or non-edge port, or enable
                      edge discovery. Edge ports are connected to a LAN that has no
                      other bridges attached.
                    type: string
                  edgePort:
                    description: Whether port is an edge port or not.
                    type: boolean
                  edgePortDiscovery:
                    description: Whether port is set to automatically detect edge
                      ports.
                    type: boolean
                  externalFdbStatus:
                    description: Whether registration table is used instead of forwarding
                      data base.
                    type: boolean
                  fastLeave:
                    description: Enables IGMP Fast leave feature on the port.
                    type: boolean
                  forwarding:
                    description: Shows if the port is not blocked by (R/M)STP.
                    type: boolean
                  frameTypes:
                    description: Specifies allowed ingress frame types on a bridge
                      port. This property only has effect when vlan-filtering is set
                      to yes.
                    type: string
                  horizon:
                    description: Use split horizon bridging to prevent bridging loops.
                      Set the same value for group of ports, to prevent them from
                      sending data to ports with the same horizon value. Split horizon
                      is a software feature that disables hardware offloading. This
                      value is integer '0'..'429496729' or 'none'.
                    type: string
                  hw:
                    description: Enable or disable Hardware Offloading of the interface.
                    type: boolean
                  hwOffload:
                    description: Hardware offloading state.
                    type: boolean
                  hwOffloadGroup:
                    description: Switch chip used by the port.
                    type: string
                  id:
                    type: string
                  inactive:
                    type: boolean
                  ingressFiltering:
                    description: Enables or disables VLAN ingress filtering, which
                      checks if the ingress port is a member of the received VLAN
                      ID in the bridge VLAN table. Should be used with frame-types
                      to specify if the ingress traffic should be tagged or untagged.
                      This property only has effect when vlan-filtering is set to
                      yes.
                    type: boolean
                  interface:
                    description: Name of the interface.
                    type: string
                  internalPathCost:
                    description: Path cost to the interface for MSTI0 inside a region.
                      This property only has effect when protocol-mode is set to mstp.
                    format: int64
                    type: integer
                  lastTopologyChange:
                    description: Last topology change timer, records time since the
                      last change.
                    type: string
                  learn:
                    description: Changes MAC learning behaviour on a bridge port
                    type: string
                  learning:
                    description: Shows whether the port is capable of learning MAC
                      addresses.
                    type: boolean
                  multicastRouter:
                    description: Changes the state of a bridge port whether IGMP membership
                      reports are going to be forwarded to this port.
                    type: string
                  mvrpApplicantState:
                    description: 'MVRP applicant options (available since RouterOS
                      7.15): - non-participant - port does not send any MRP messages;
                      - normal-participant - port participates normally in MRP exchanges.'
                    type: string
                  mvrpRegistrarState:
                    description: 'MVRP registrar options (available since RouterOS
                      7.15): - fixed - port ignores all MRP messages, and remains
                      Registered (IN) in all configured vlans. - normal - port receives
                      MRP messages and handles them according to the standard.'
                    type: string
                  nextid:
                    type: string
                  pathCost:
                    description: Path cost to the interface, used by STP to determine
                      the "best" path, used by MSTP todetermine "best" path between
                      regions. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  pointToPoint:
                    description: Specifies if a bridge port is connected to a bridge
                      using a point-to-point link for faster convergence in case of
                      failure. This property has no effect when protocol-mode is set
                      to none.
                    type: string
                  pointToPointPort:
                    description: Whether the port is connected to a bridge port using
                      full-duplex (true) or half-duplex (false).
                    type: boolean
                  portId:
                    description: In Spanning Tree Protocol each port has a unique
                      Port Identifier. Priority[hex] + port number.
                    type: string
                  priority:
                    description: The priority of the interface, used by STP to determine
                      the root port, used by MSTP to determine root port between regions.
                    type: string
                  pvid:
                    description: ort VLAN ID (pvid) specifies which VLAN the untagged
                      ingress traffic is assigned to. This property only has effect
                      when vlan-filtering is set to yes.
                    format: int64
                    type: integer
                  restrictedRole:
                    description: Enable the restricted role on a port, used by STP
                      to forbid a port becoming a root port. This property only has
                      effect when protocol-mode is set to mstp.
                    type: boolean
                  restrictedTcn:
                    description: Disable topology change notification (TCN) sending
                      on a port, used by STP to forbid network topology changes to
                      propagate. This property only has effect when protocol-mode
                      is set to mstp.
                    type: boolean
                  role:
                    description: (R/M)STP algorithm assigned role of the port
                    type: string
                  rootPathCost:
                    description: The total cost of the path to the root-bridge.
                    format: int64
                    type: integer
                  sendingRstp:
                    description: Whether the port is sending RSTP or MSTP BPDU types.
                      A port will transit to STP type when RSTP/MSTP enabled port
                      receives a STP BPDU
                    type: string
                  status:
                    description: Port status ('in-bridge' - port is enabled).
                    type: string
                  tagStacking:
                    description: Forces all packets to be treated as untagged packets.
                      Packets on ingress port will be tagged with another VLAN tag
                      regardless if a VLAN tag already exists, packets will be tagged
                      with a VLAN ID that matches the pvid value and will use EtherType
                      that is specified in ether-type. This property only has effect
                      when vlan-filtering is set to yes.
                    type: boolean
                  trusted:
                    description: When enabled, it allows to forward DHCP packets towards
                      DHCP server through this port. Mainly used to limit unauthorized
                      servers to provide malicious information for users. This property
                      only has effect when dhcp-snooping is set to yes.
                    type: boolean
                  unknownMulticastFlood:
                    description: When enabled, bridge floods unknown multicast traffic
                      to all bridge egress ports.
                    type: boolean
                  unknownUnicastFlood:
                    description: When enabled, bridge floods unknown unicast traffic
                      to all bridge egress ports.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
