---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: accesslists.capsman.routeros.m.sindrip.io
spec:
  group: capsman.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: AccessList
    listKind: AccessListList
    plural: accesslists
    singular: accesslist
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessList is the Schema for the AccessLists API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessListSpec defines the desired state of AccessList
            properties:
              forProvider:
                properties:
                  action:
                    description: |-
                      (String) An action to take when a client matches.
                      An action to take when a client matches.
                    type: string
                  allowSignalOutOfRange:
                    description: |-
                      (String) An option that permits the client's signal to be out of the range always or for some time interval.
                      An option that permits the client's signal to be out of the range always or for some time interval.
                    type: string
                  apTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the client..
                      Transmission speed limit in the direction of the client..
                    format: int64
                    type: integer
                  clientToClientForwarding:
                    description: |-
                      (Boolean) An option that specifies whether to allow forwarding data between clients connected to the same interface.
                      An option that specifies whether to allow forwarding data between clients connected to the same interface.
                    type: boolean
                  clientTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the access point.
                      Transmission speed limit in the direction of the access point.
                    format: int64
                    type: integer
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  interface:
                    description: |-
                      (String) Interface name to compare with an interface to which the client actually connects to.
                      Interface name to compare with an interface to which the client actually connects to.
                    type: string
                  macAddress:
                    description: |-
                      (String) MAC address of the client.
                      MAC address of the client.
                    type: string
                  macMask:
                    description: |-
                      (String) MAC address mask to apply when comparing clients' addresses.
                      MAC address mask to apply when comparing clients' addresses.
                    type: string
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  privatePassphrase:
                    description: |-
                      (String) PSK passphrase for the client if some PSK authentication algorithm is used.
                      PSK passphrase for the client if some PSK authentication algorithm is used.
                    type: string
                  radiusAccounting:
                    description: |-
                      (Boolean) An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                      An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                    type: boolean
                  signalRange:
                    description: |-
                      (String) The range in which the client signal must fall.
                      The range in which the client signal must fall.
                    type: string
                  ssidRegexp:
                    description: |-
                      (String) The regular expression to compare the actual SSID the client connects to.
                      The regular expression to compare the actual SSID the client connects to.
                    type: string
                  time:
                    description: |-
                      (String) Time of the day and days of the week when the rule is applicable.
                      Time of the day and days of the week when the rule is applicable.
                    type: string
                  vlanId:
                    description: |-
                      mode enables use of VLAN tagging.
                      VLAN ID to use if vlan-mode enables use of VLAN tagging.
                    format: int64
                    type: integer
                  vlanMode:
                    description: |-
                      (String) VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                      VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: |-
                      (String) An action to take when a client matches.
                      An action to take when a client matches.
                    type: string
                  allowSignalOutOfRange:
                    description: |-
                      (String) An option that permits the client's signal to be out of the range always or for some time interval.
                      An option that permits the client's signal to be out of the range always or for some time interval.
                    type: string
                  apTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the client..
                      Transmission speed limit in the direction of the client..
                    format: int64
                    type: integer
                  clientToClientForwarding:
                    description: |-
                      (Boolean) An option that specifies whether to allow forwarding data between clients connected to the same interface.
                      An option that specifies whether to allow forwarding data between clients connected to the same interface.
                    type: boolean
                  clientTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the access point.
                      Transmission speed limit in the direction of the access point.
                    format: int64
                    type: integer
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  interface:
                    description: |-
                      (String) Interface name to compare with an interface to which the client actually connects to.
                      Interface name to compare with an interface to which the client actually connects to.
                    type: string
                  macAddress:
                    description: |-
                      (String) MAC address of the client.
                      MAC address of the client.
                    type: string
                  macMask:
                    description: |-
                      (String) MAC address mask to apply when comparing clients' addresses.
                      MAC address mask to apply when comparing clients' addresses.
                    type: string
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  privatePassphrase:
                    description: |-
                      (String) PSK passphrase for the client if some PSK authentication algorithm is used.
                      PSK passphrase for the client if some PSK authentication algorithm is used.
                    type: string
                  radiusAccounting:
                    description: |-
                      (Boolean) An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                      An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                    type: boolean
                  signalRange:
                    description: |-
                      (String) The range in which the client signal must fall.
                      The range in which the client signal must fall.
                    type: string
                  ssidRegexp:
                    description: |-
                      (String) The regular expression to compare the actual SSID the client connects to.
                      The regular expression to compare the actual SSID the client connects to.
                    type: string
                  time:
                    description: |-
                      (String) Time of the day and days of the week when the rule is applicable.
                      Time of the day and days of the week when the rule is applicable.
                    type: string
                  vlanId:
                    description: |-
                      mode enables use of VLAN tagging.
                      VLAN ID to use if vlan-mode enables use of VLAN tagging.
                    format: int64
                    type: integer
                  vlanMode:
                    description: |-
                      (String) VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                      VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AccessListStatus defines the observed state of AccessList.
            properties:
              atProvider:
                properties:
                  action:
                    description: |-
                      (String) An action to take when a client matches.
                      An action to take when a client matches.
                    type: string
                  allowSignalOutOfRange:
                    description: |-
                      (String) An option that permits the client's signal to be out of the range always or for some time interval.
                      An option that permits the client's signal to be out of the range always or for some time interval.
                    type: string
                  apTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the client..
                      Transmission speed limit in the direction of the client..
                    format: int64
                    type: integer
                  clientToClientForwarding:
                    description: |-
                      (Boolean) An option that specifies whether to allow forwarding data between clients connected to the same interface.
                      An option that specifies whether to allow forwarding data between clients connected to the same interface.
                    type: boolean
                  clientTxLimit:
                    description: |-
                      (Number) Transmission speed limit in the direction of the access point.
                      Transmission speed limit in the direction of the access point.
                    format: int64
                    type: integer
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interface:
                    description: |-
                      (String) Interface name to compare with an interface to which the client actually connects to.
                      Interface name to compare with an interface to which the client actually connects to.
                    type: string
                  macAddress:
                    description: |-
                      (String) MAC address of the client.
                      MAC address of the client.
                    type: string
                  macMask:
                    description: |-
                      (String) MAC address mask to apply when comparing clients' addresses.
                      MAC address mask to apply when comparing clients' addresses.
                    type: string
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  privatePassphrase:
                    description: |-
                      (String) PSK passphrase for the client if some PSK authentication algorithm is used.
                      PSK passphrase for the client if some PSK authentication algorithm is used.
                    type: string
                  radiusAccounting:
                    description: |-
                      (Boolean) An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                      An option that specifies if RADIUS traffic accounting should be used in case of RADIUS authentication of the client.
                    type: boolean
                  signalRange:
                    description: |-
                      (String) The range in which the client signal must fall.
                      The range in which the client signal must fall.
                    type: string
                  ssidRegexp:
                    description: |-
                      (String) The regular expression to compare the actual SSID the client connects to.
                      The regular expression to compare the actual SSID the client connects to.
                    type: string
                  time:
                    description: |-
                      (String) Time of the day and days of the week when the rule is applicable.
                      Time of the day and days of the week when the rule is applicable.
                    type: string
                  vlanId:
                    description: |-
                      mode enables use of VLAN tagging.
                      VLAN ID to use if vlan-mode enables use of VLAN tagging.
                    format: int64
                    type: integer
                  vlanMode:
                    description: |-
                      (String) VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                      VLAN tagging mode specifies if traffic coming from a client should get tagged and untagged when it goes back to the client.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
