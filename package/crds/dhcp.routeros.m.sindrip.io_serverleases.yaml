---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: serverleases.dhcp.routeros.m.sindrip.io
spec:
  group: dhcp.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: ServerLease
    listKind: ServerLeaseList
    plural: serverleases
    singular: serverlease
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServerLease is the Schema for the ServerLeases API. #### This
          is an alias for backwards compatibility between plugin versions. Please
          see documentation for [routeros_ip_dhcp_server_lease](ip_dhcp_server_lease.md)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerLeaseSpec defines the desired state of ServerLease
            properties:
              forProvider:
                properties:
                  address:
                    description: The IP address of the DHCP lease to be created.
                    type: string
                  addressLists:
                    description: Address list to which address will be added if lease
                      is bound.
                    type: string
                  allowDualStackQueue:
                    description: Creates a single simple queue entry for both IPv4
                      and IPv6 addresses, uses the MAC address and DUID for identification.
                    type: boolean
                  alwaysBroadcast:
                    description: Send all replies as broadcasts.
                    type: boolean
                  blockAccess:
                    description: Whether to block access for this DHCP client (true|false).
                    type: boolean
                  clientId:
                    description: If specified, must match DHCP 'client identifier'
                      option of the request.
                    type: string
                  comment:
                    type: string
                  dhcpOption:
                    description: Add additional DHCP options.
                    type: string
                  dhcpOptionSet:
                    description: Add additional set of DHCP options.
                    type: string
                  disabled:
                    type: boolean
                  insertQueueBefore:
                    description: Specify where to place dynamic simple queue entries
                      for static DCHP leases with rate-limit parameter set.
                    type: string
                  leaseTime:
                    description: Time that the client may use the address. If set
                      to 0s lease will never expire.
                    type: string
                  macAddress:
                    description: The MAC addreess of the DHCP lease to be created.
                    type: string
                  rateLimit:
                    description: Adds a dynamic simple queue to limit IP's bandwidth
                      to a specified rate. Requires the lease to be static.
                    type: string
                  server:
                    description: Server name which serves this client.
                    type: string
                  useSrcMac:
                    description: When this option is set server uses source MAC address
                      instead of received CHADDR to assign address.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  address:
                    description: The IP address of the DHCP lease to be created.
                    type: string
                  addressLists:
                    description: Address list to which address will be added if lease
                      is bound.
                    type: string
                  allowDualStackQueue:
                    description: Creates a single simple queue entry for both IPv4
                      and IPv6 addresses, uses the MAC address and DUID for identification.
                    type: boolean
                  alwaysBroadcast:
                    description: Send all replies as broadcasts.
                    type: boolean
                  blockAccess:
                    description: Whether to block access for this DHCP client (true|false).
                    type: boolean
                  clientId:
                    description: If specified, must match DHCP 'client identifier'
                      option of the request.
                    type: string
                  comment:
                    type: string
                  dhcpOption:
                    description: Add additional DHCP options.
                    type: string
                  dhcpOptionSet:
                    description: Add additional set of DHCP options.
                    type: string
                  disabled:
                    type: boolean
                  insertQueueBefore:
                    description: Specify where to place dynamic simple queue entries
                      for static DCHP leases with rate-limit parameter set.
                    type: string
                  leaseTime:
                    description: Time that the client may use the address. If set
                      to 0s lease will never expire.
                    type: string
                  macAddress:
                    description: The MAC addreess of the DHCP lease to be created.
                    type: string
                  rateLimit:
                    description: Adds a dynamic simple queue to limit IP's bandwidth
                      to a specified rate. Requires the lease to be static.
                    type: string
                  server:
                    description: Server name which serves this client.
                    type: string
                  useSrcMac:
                    description: When this option is set server uses source MAC address
                      instead of received CHADDR to assign address.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.address is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.address)
                || (has(self.initProvider) && has(self.initProvider.address))'
            - message: spec.forProvider.macAddress is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.macAddress)
                || (has(self.initProvider) && has(self.initProvider.macAddress))'
          status:
            description: ServerLeaseStatus defines the observed state of ServerLease.
            properties:
              atProvider:
                properties:
                  activeAddress:
                    description: The IP address of the machine currently holding the
                      DHCP lease.
                    type: string
                  activeClientId:
                    description: Actual client-id of the client.
                    type: string
                  activeHostname:
                    description: The hostname of the machine currently holding the
                      DHCP lease.
                    type: string
                  activeMacAddress:
                    description: The MAC address of of the machine currently holding
                      the DHCP lease.
                    type: string
                  activeServer:
                    description: Actual dhcp server, which serves this client.
                    type: string
                  address:
                    description: The IP address of the DHCP lease to be created.
                    type: string
                  addressLists:
                    description: Address list to which address will be added if lease
                      is bound.
                    type: string
                  agentCircuitId:
                    description: Circuit ID of DHCP relay agent. If each character
                      should be valid ASCII text symbol or else this value is displayed
                      as hex dump.
                    type: string
                  agentRemoteId:
                    description: Remote ID, set by DHCP relay agent.
                    type: string
                  allowDualStackQueue:
                    description: Creates a single simple queue entry for both IPv4
                      and IPv6 addresses, uses the MAC address and DUID for identification.
                    type: boolean
                  alwaysBroadcast:
                    description: Send all replies as broadcasts.
                    type: boolean
                  blockAccess:
                    description: Whether to block access for this DHCP client (true|false).
                    type: boolean
                  blocked:
                    description: Whether the lease is blocked.
                    type: boolean
                  classId:
                    description: Class ID of the client. This option is available
                      in RouterOS starting from version 7.16.
                    type: string
                  clientId:
                    description: If specified, must match DHCP 'client identifier'
                      option of the request.
                    type: string
                  comment:
                    type: string
                  dhcpOption:
                    description: Add additional DHCP options.
                    type: string
                  dhcpOptionSet:
                    description: Add additional set of DHCP options.
                    type: string
                  disabled:
                    type: boolean
                  dynamic:
                    description: Whether the dhcp lease is static or dynamic. Dynamic
                      leases are not guaranteed to continue to be assigned to that
                      specific device.
                    type: boolean
                  expiresAfter:
                    description: Time until lease expires.
                    type: string
                  hostName:
                    description: The hostname of the device
                    type: string
                  id:
                    type: string
                  insertQueueBefore:
                    description: Specify where to place dynamic simple queue entries
                      for static DCHP leases with rate-limit parameter set.
                    type: string
                  lastSeen:
                    type: string
                  leaseTime:
                    description: Time that the client may use the address. If set
                      to 0s lease will never expire.
                    type: string
                  macAddress:
                    description: The MAC addreess of the DHCP lease to be created.
                    type: string
                  radius:
                    description: Shows if this dynamic lease is authenticated by RADIUS
                      or not.
                    type: string
                  rateLimit:
                    description: Adds a dynamic simple queue to limit IP's bandwidth
                      to a specified rate. Requires the lease to be static.
                    type: string
                  server:
                    description: Server name which serves this client.
                    type: string
                  srcMacAddress:
                    description: Source MAC address.
                    type: string
                  status:
                    description: Lease status.
                    type: string
                  useSrcMac:
                    description: When this option is set server uses source MAC address
                      instead of received CHADDR to assign address.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
