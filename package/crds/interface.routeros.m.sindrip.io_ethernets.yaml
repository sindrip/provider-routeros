---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ethernets.interface.routeros.m.sindrip.io
spec:
  group: interface.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: Ethernet
    listKind: EthernetList
    plural: ethernets
    singular: ethernet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Ethernet is the Schema for the Ethernets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EthernetSpec defines the desired state of Ethernet
            properties:
              forProvider:
                properties:
                  advertise:
                    description: |-
                      (String) Advertised speed and duplex modes for Ethernet interfaces over twisted pair,

                      Advertised speed and duplex modes for Ethernet interfaces over twisted pair,
                      only applies when auto-negotiation is enabled. Advertising higher speeds than
                      the actual interface supported speed will have no effect, multiple options are allowed.
                    type: string
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  autoNegotiation:
                    description: |-
                      (Boolean) When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      Note1: Auto-negotiation should not be disabled on one end only, otherwise Ethernet Interfaces may not work properly.
                      Note2: Gigabit Ethernet and NBASE-T Ethernet links cannot work with auto-negotiation disabled.
                    type: boolean
                  bandwidth:
                    description: |-
                      chip ports.
                      Sets max rx/tx bandwidth in kbps that will be handled by an interface. TX limit is supported on all Atheros switch-chip ports.
                      RX limit is supported only on Atheros8327/QCA8337 switch-chip ports.
                    type: string
                  cableSettings:
                    description: |-
                      (String) Changes the cable length setting (only applicable to NS DP83815/6 cards)
                      Changes the cable length setting (only applicable to NS DP83815/6 cards)
                    type: string
                  comboMode:
                    description: |-
                      (String) When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      the priority will be the SFP/SFP+ port. When sfp mode is selected, the interface will only work through SFP/SFP+ cage.
                      When copper mode is selected, the interface will only work through RJ45 Ethernet port.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disableRunningCheck:
                    description: |-
                      (Boolean) Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Default value is 'yes' because older NICs do not support it. (only applicable to x86)
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  factoryName:
                    description: |-
                      (String) The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                      The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                    type: string
                  fecMode:
                    description: |-
                      working link or even false link-ups.
                      Changes Forward Error Correction (FEC) mode for SFP28, QSFP+ and QSFP28 interfaces. Same mode should be used on both link ends, otherwise FEC mismatch could result in non-working link or even false link-ups.
                    type: string
                  fullDuplex:
                    description: |-
                      negotiation is disabled.
                      Defines whether the transmission of data appears in two directions simultaneously, only applies when auto-negotiation is disabled.
                    type: boolean
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  loopProtect:
                    description: (String)
                    type: string
                  loopProtectDisableTime:
                    description: (String)
                    type: string
                  loopProtectSendInterval:
                    description: (String)
                    type: string
                  macAddress:
                    description: |-
                      (String) Media Access Control number of an interface.
                      Media Access Control number of an interface.
                    type: string
                  mdixEnable:
                    description: |-
                      (Boolean) Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                      Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                    type: boolean
                  mtu:
                    description: |-
                      (Number) Layer3 Maximum transmission unit
                      Layer3 Maximum transmission unit
                    format: int64
                    type: integer
                  name:
                    description: |-
                      (String) Name of the ethernet interface.
                      Name of the ethernet interface.
                    type: string
                  poeLldpEnabled:
                    description: |-
                      (Boolean) An option that enables LLDP for managing devices.
                      An option that enables LLDP for managing devices.
                    type: boolean
                  poeOut:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    type: string
                  poePriority:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    format: int64
                    type: integer
                  poeVoltage:
                    description: |-
                      (String) An option that allows us to manually control the voltage outputs on the PoE port.
                      An option that allows us to manually control the voltage outputs on the PoE port.
                    type: string
                  powerCycleInterval:
                    description: |-
                      Out power for 5s between the specified intervals.
                      An options that disables PoE-Out power for 5s between the specified intervals.
                    type: string
                  powerCyclePingAddress:
                    description: |-
                      (String) An address to monitor.
                      An address to monitor.
                    type: string
                  powerCyclePingEnabled:
                    description: |-
                      Telnet packets.
                      An option that enables ping watchdog of power cycles on the port if a host does not respond to ICMP or MAC-Telnet packets.
                    type: boolean
                  powerCyclePingTimeout:
                    description: |-
                      Out port is switched off for 5s.
                      If the host does not respond over the specified period, the PoE-Out port is switched off for 5s.
                    type: string
                  rxFlowControl:
                    description: |-
                      (String) When set to on, the port will process received pause frames and suspend transmission if required.
                      When set to on, the port will process received pause frames and suspend transmission if required.
                      auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                  sfpIgnoreRxLos:
                    description: |-
                      (Boolean) An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                      An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                    type: boolean
                  sfpRateSelect:
                    description: |-
                      (String) Allows to control rate select pin for SFP ports. Values: high | low
                      Allows to control rate select pin for SFP ports. Values: high | low
                    type: string
                  sfpShutdownTemperature:
                    description: |-
                      (Number) The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                      The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                    format: int64
                    type: integer
                  speed:
                    description: |-
                      (String) Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                      Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                    type: string
                  txFlowControl:
                    description: |-
                      (String) When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      Pause frames are only generated when some routers output interface is congested and packets cannot be transmitted anymore.
                      Auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  advertise:
                    description: |-
                      (String) Advertised speed and duplex modes for Ethernet interfaces over twisted pair,

                      Advertised speed and duplex modes for Ethernet interfaces over twisted pair,
                      only applies when auto-negotiation is enabled. Advertising higher speeds than
                      the actual interface supported speed will have no effect, multiple options are allowed.
                    type: string
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  autoNegotiation:
                    description: |-
                      (Boolean) When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      Note1: Auto-negotiation should not be disabled on one end only, otherwise Ethernet Interfaces may not work properly.
                      Note2: Gigabit Ethernet and NBASE-T Ethernet links cannot work with auto-negotiation disabled.
                    type: boolean
                  bandwidth:
                    description: |-
                      chip ports.
                      Sets max rx/tx bandwidth in kbps that will be handled by an interface. TX limit is supported on all Atheros switch-chip ports.
                      RX limit is supported only on Atheros8327/QCA8337 switch-chip ports.
                    type: string
                  cableSettings:
                    description: |-
                      (String) Changes the cable length setting (only applicable to NS DP83815/6 cards)
                      Changes the cable length setting (only applicable to NS DP83815/6 cards)
                    type: string
                  comboMode:
                    description: |-
                      (String) When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      the priority will be the SFP/SFP+ port. When sfp mode is selected, the interface will only work through SFP/SFP+ cage.
                      When copper mode is selected, the interface will only work through RJ45 Ethernet port.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disableRunningCheck:
                    description: |-
                      (Boolean) Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Default value is 'yes' because older NICs do not support it. (only applicable to x86)
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  factoryName:
                    description: |-
                      (String) The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                      The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                    type: string
                  fecMode:
                    description: |-
                      working link or even false link-ups.
                      Changes Forward Error Correction (FEC) mode for SFP28, QSFP+ and QSFP28 interfaces. Same mode should be used on both link ends, otherwise FEC mismatch could result in non-working link or even false link-ups.
                    type: string
                  fullDuplex:
                    description: |-
                      negotiation is disabled.
                      Defines whether the transmission of data appears in two directions simultaneously, only applies when auto-negotiation is disabled.
                    type: boolean
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  loopProtect:
                    description: (String)
                    type: string
                  loopProtectDisableTime:
                    description: (String)
                    type: string
                  loopProtectSendInterval:
                    description: (String)
                    type: string
                  macAddress:
                    description: |-
                      (String) Media Access Control number of an interface.
                      Media Access Control number of an interface.
                    type: string
                  mdixEnable:
                    description: |-
                      (Boolean) Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                      Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                    type: boolean
                  mtu:
                    description: |-
                      (Number) Layer3 Maximum transmission unit
                      Layer3 Maximum transmission unit
                    format: int64
                    type: integer
                  name:
                    description: |-
                      (String) Name of the ethernet interface.
                      Name of the ethernet interface.
                    type: string
                  poeLldpEnabled:
                    description: |-
                      (Boolean) An option that enables LLDP for managing devices.
                      An option that enables LLDP for managing devices.
                    type: boolean
                  poeOut:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    type: string
                  poePriority:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    format: int64
                    type: integer
                  poeVoltage:
                    description: |-
                      (String) An option that allows us to manually control the voltage outputs on the PoE port.
                      An option that allows us to manually control the voltage outputs on the PoE port.
                    type: string
                  powerCycleInterval:
                    description: |-
                      Out power for 5s between the specified intervals.
                      An options that disables PoE-Out power for 5s between the specified intervals.
                    type: string
                  powerCyclePingAddress:
                    description: |-
                      (String) An address to monitor.
                      An address to monitor.
                    type: string
                  powerCyclePingEnabled:
                    description: |-
                      Telnet packets.
                      An option that enables ping watchdog of power cycles on the port if a host does not respond to ICMP or MAC-Telnet packets.
                    type: boolean
                  powerCyclePingTimeout:
                    description: |-
                      Out port is switched off for 5s.
                      If the host does not respond over the specified period, the PoE-Out port is switched off for 5s.
                    type: string
                  rxFlowControl:
                    description: |-
                      (String) When set to on, the port will process received pause frames and suspend transmission if required.
                      When set to on, the port will process received pause frames and suspend transmission if required.
                      auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                  sfpIgnoreRxLos:
                    description: |-
                      (Boolean) An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                      An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                    type: boolean
                  sfpRateSelect:
                    description: |-
                      (String) Allows to control rate select pin for SFP ports. Values: high | low
                      Allows to control rate select pin for SFP ports. Values: high | low
                    type: string
                  sfpShutdownTemperature:
                    description: |-
                      (Number) The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                      The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                    format: int64
                    type: integer
                  speed:
                    description: |-
                      (String) Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                      Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                    type: string
                  txFlowControl:
                    description: |-
                      (String) When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      Pause frames are only generated when some routers output interface is congested and packets cannot be transmitted anymore.
                      Auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.factoryName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.factoryName)
                || (has(self.initProvider) && has(self.initProvider.factoryName))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: EthernetStatus defines the observed state of Ethernet.
            properties:
              atProvider:
                properties:
                  advertise:
                    description: |-
                      (String) Advertised speed and duplex modes for Ethernet interfaces over twisted pair,

                      Advertised speed and duplex modes for Ethernet interfaces over twisted pair,
                      only applies when auto-negotiation is enabled. Advertising higher speeds than
                      the actual interface supported speed will have no effect, multiple options are allowed.
                    type: string
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  autoNegotiation:
                    description: |-
                      (Boolean) When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      When enabled, the interface "advertises" its maximum capabilities to achieve the best connection possible.
                      Note1: Auto-negotiation should not be disabled on one end only, otherwise Ethernet Interfaces may not work properly.
                      Note2: Gigabit Ethernet and NBASE-T Ethernet links cannot work with auto-negotiation disabled.
                    type: boolean
                  bandwidth:
                    description: |-
                      chip ports.
                      Sets max rx/tx bandwidth in kbps that will be handled by an interface. TX limit is supported on all Atheros switch-chip ports.
                      RX limit is supported only on Atheros8327/QCA8337 switch-chip ports.
                    type: string
                  cableSettings:
                    description: |-
                      (String) Changes the cable length setting (only applicable to NS DP83815/6 cards)
                      Changes the cable length setting (only applicable to NS DP83815/6 cards)
                    type: string
                  comboMode:
                    description: |-
                      (String) When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      When auto mode is selected, the port that was first connected will establish the link. In case this link fails, the other port will try to establish a new link. If both ports are connected at the same time (e.g. after reboot),
                      the priority will be the SFP/SFP+ port. When sfp mode is selected, the interface will only work through SFP/SFP+ cage.
                      When copper mode is selected, the interface will only work through RJ45 Ethernet port.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  defaultName:
                    description: |-
                      (String) The default name for an interface.
                      The default name for an interface.
                    type: string
                  disableRunningCheck:
                    description: |-
                      (Boolean) Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Disable running check. If this value is set to 'no', the router automatically detects whether the NIC is connected with a device in the network or not.
                      Default value is 'yes' because older NICs do not support it. (only applicable to x86)
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  factoryName:
                    description: |-
                      (String) The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                      The factory name of the identifier, serves as resource identifier. Determines which interface will be updated.
                    type: string
                  fecMode:
                    description: |-
                      working link or even false link-ups.
                      Changes Forward Error Correction (FEC) mode for SFP28, QSFP+ and QSFP28 interfaces. Same mode should be used on both link ends, otherwise FEC mismatch could result in non-working link or even false link-ups.
                    type: string
                  fullDuplex:
                    description: |-
                      negotiation is disabled.
                      Defines whether the transmission of data appears in two directions simultaneously, only applies when auto-negotiation is disabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  loopProtect:
                    description: (String)
                    type: string
                  loopProtectDisableTime:
                    description: (String)
                    type: string
                  loopProtectSendInterval:
                    description: (String)
                    type: string
                  loopProtectStatus:
                    description: (String)
                    type: string
                  macAddress:
                    description: |-
                      (String) Media Access Control number of an interface.
                      Media Access Control number of an interface.
                    type: string
                  mdixEnable:
                    description: |-
                      (Boolean) Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                      Whether the MDI/X auto cross over cable correction feature is enabled for the port (Hardware specific, e.g. ether1 on RB500 can be set to yes/no. Fixed to 'yes' on other hardware.)
                    type: boolean
                  mtu:
                    description: |-
                      (Number) Layer3 Maximum transmission unit
                      Layer3 Maximum transmission unit
                    format: int64
                    type: integer
                  name:
                    description: |-
                      (String) Name of the ethernet interface.
                      Name of the ethernet interface.
                    type: string
                  origMacAddress:
                    description: |-
                      (String) Original Media Access Control number of an interface. (read only)
                      Original Media Access Control number of an interface. (read only)
                    type: string
                  poeLldpEnabled:
                    description: |-
                      (Boolean) An option that enables LLDP for managing devices.
                      An option that enables LLDP for managing devices.
                    type: boolean
                  poeOut:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    type: string
                  poePriority:
                    description: |-
                      Out)
                      PoE settings: (https://wiki.mikrotik.com/wiki/Manual:PoE-Out)
                    format: int64
                    type: integer
                  poeVoltage:
                    description: |-
                      (String) An option that allows us to manually control the voltage outputs on the PoE port.
                      An option that allows us to manually control the voltage outputs on the PoE port.
                    type: string
                  powerCycleInterval:
                    description: |-
                      Out power for 5s between the specified intervals.
                      An options that disables PoE-Out power for 5s between the specified intervals.
                    type: string
                  powerCyclePingAddress:
                    description: |-
                      (String) An address to monitor.
                      An address to monitor.
                    type: string
                  powerCyclePingEnabled:
                    description: |-
                      Telnet packets.
                      An option that enables ping watchdog of power cycles on the port if a host does not respond to ICMP or MAC-Telnet packets.
                    type: boolean
                  powerCyclePingTimeout:
                    description: |-
                      Out port is switched off for 5s.
                      If the host does not respond over the specified period, the PoE-Out port is switched off for 5s.
                    type: string
                  running:
                    description: |-
                      (Boolean) Whether interface is running. Note that some interface does not have running check and they are always reported as "running"
                      Whether interface is running. Note that some interface does not have running check and they are always reported as "running"
                    type: boolean
                  rxFlowControl:
                    description: |-
                      (String) When set to on, the port will process received pause frames and suspend transmission if required.
                      When set to on, the port will process received pause frames and suspend transmission if required.
                      auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                  sfpIgnoreRxLos:
                    description: |-
                      (Boolean) An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                      An option to ignore RX LOS (Loss of Signal) status of the SFP module.
                    type: boolean
                  sfpRateSelect:
                    description: |-
                      (String) Allows to control rate select pin for SFP ports. Values: high | low
                      Allows to control rate select pin for SFP ports. Values: high | low
                    type: string
                  sfpShutdownTemperature:
                    description: |-
                      (Number) The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                      The temperature in Celsius at which the interface will be temporarily turned off due to too high detected SFP module temperature (introduced v6.48).The default value for SFP/SFP+/SFP28 interfaces is 95, and for QSFP+/QSFP28 interfaces 80 (introduced v7.6).
                    format: int64
                    type: integer
                  slave:
                    description: |-
                      (Boolean) Whether interface is configured as a slave of another interface (for example Bonding)
                      Whether interface is configured as a slave of another interface (for example Bonding)
                    type: boolean
                  speed:
                    description: |-
                      (String) Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                      Sets interface data transmission speed which takes effect only when ```auto_negotiation``` is disabled.
                    type: string
                  switch:
                    description: |-
                      (String) ID to which switch chip interface belongs to.
                      ID to which switch chip interface belongs to.
                    type: string
                  txFlowControl:
                    description: |-
                      (String) When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      When set to on, the port will generate pause frames to the upstream device to temporarily stop the packet transmission.
                      Pause frames are only generated when some routers output interface is congested and packets cannot be transmitted anymore.
                      Auto is the same as on except when auto-negotiation=yes flow control status is resolved by taking into account what other end advertises.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
