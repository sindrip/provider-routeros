---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ethernetswitchcrs.interface.routeros.m.sindrip.io
spec:
  group: interface.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: EthernetSwitchCrs
    listKind: EthernetSwitchCrsList
    plural: ethernetswitchcrs
    singular: ethernetswitchcrs
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EthernetSwitchCrs is the Schema for the EthernetSwitchCrss API.
          Resource for managing CRS (Cloud Router Switch) series device properties.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EthernetSwitchCrsSpec defines the desired state of EthernetSwitchCrs
            properties:
              forProvider:
                properties:
                  bridgeType:
                    description: |-
                      VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                      The bridge type defines which VLAN tag is used as Lookup-VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                    type: string
                  bypassIngressPortPolicingFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress Port Policing. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassL2SecurityCheckFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Policy rule security check. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassVlanIngressFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress VLAN filtering. These protocols are not dropped if they have invalid VLAN. (arp, dhcpv4, dhcpv6,eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfInvalidOrSrcPortNotMemberOfVlanOnPorts:
                    description: |-
                      (Set of String) Ports that drop invalid and other port VLAN ID frames.
                      Ports that drop invalid and other port VLAN ID frames.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfNoVlanAssignmentOnPorts:
                    description: |-
                      based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                      Ports which drop frames if no MAC-based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirrorRatio:
                    description: |-
                      (String) Proportion of egress mirrored packets compared to all packets.
                      Proportion of egress mirrored packets compared to all packets.
                    type: string
                  fdbUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for FDB-based mirroring.
                    type: string
                  forwardUnknownVlan:
                    description: |-
                      (Boolean) Whether to allow forwarding VLANs that are not members of the VLAN table.
                      Whether to allow forwarding VLANs that are not members of the VLAN table.
                    type: boolean
                  ingressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirrorRatio:
                    description: |-
                      (String) The proportion of ingress mirrored packets compared to all packets.
                      The proportion of ingress mirrored packets compared to all packets.
                    type: string
                  macLevelIsolation:
                    description: |-
                      profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                      Globally enables or disables MAC level isolation. Once enabled, the switch will check the source and destination MAC address entries and their isolation-profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                    type: boolean
                  mirrorEgressIfIngressMirrored:
                    description: |-
                      (Boolean) When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                      When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                    type: boolean
                  mirrorTxOnMirrorPort:
                    description: (Boolean)
                    type: boolean
                  mirroredPacketDropPrecedence:
                    description: |-
                      (String) Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                      Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                    type: string
                  mirroredPacketQosPriority:
                    description: |-
                      (Number) Remarked priority in mirrored packets.
                      Remarked priority in mirrored packets.
                    format: int64
                    type: integer
                  multicastLookupMode:
                    description: |-
                      mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                      Lookup mode for IPv4 multicast bridging.dst-mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                    type: string
                  name:
                    description: |-
                      (String) Name of the switch.
                      Name of the switch.
                    type: string
                  overrideExistingWhenUfdbFull:
                    description: |-
                      (Boolean) Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                      Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                    type: boolean
                  unicastFdbTimeout:
                    description: |-
                      (String) Timeout for Unicast FDB entries.
                      Timeout for Unicast FDB entries.
                    type: string
                  unknownVlanLookupMode:
                    description: |-
                      (String) Lookup and learning mode for packets with invalid VLAN.
                      Lookup and learning mode for packets with invalid VLAN.
                    type: string
                  useCvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  useSvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  vlanUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for VLAN-based mirroring.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  bridgeType:
                    description: |-
                      VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                      The bridge type defines which VLAN tag is used as Lookup-VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                    type: string
                  bypassIngressPortPolicingFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress Port Policing. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassL2SecurityCheckFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Policy rule security check. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassVlanIngressFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress VLAN filtering. These protocols are not dropped if they have invalid VLAN. (arp, dhcpv4, dhcpv6,eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfInvalidOrSrcPortNotMemberOfVlanOnPorts:
                    description: |-
                      (Set of String) Ports that drop invalid and other port VLAN ID frames.
                      Ports that drop invalid and other port VLAN ID frames.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfNoVlanAssignmentOnPorts:
                    description: |-
                      based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                      Ports which drop frames if no MAC-based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirrorRatio:
                    description: |-
                      (String) Proportion of egress mirrored packets compared to all packets.
                      Proportion of egress mirrored packets compared to all packets.
                    type: string
                  fdbUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for FDB-based mirroring.
                    type: string
                  forwardUnknownVlan:
                    description: |-
                      (Boolean) Whether to allow forwarding VLANs that are not members of the VLAN table.
                      Whether to allow forwarding VLANs that are not members of the VLAN table.
                    type: boolean
                  ingressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirrorRatio:
                    description: |-
                      (String) The proportion of ingress mirrored packets compared to all packets.
                      The proportion of ingress mirrored packets compared to all packets.
                    type: string
                  macLevelIsolation:
                    description: |-
                      profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                      Globally enables or disables MAC level isolation. Once enabled, the switch will check the source and destination MAC address entries and their isolation-profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                    type: boolean
                  mirrorEgressIfIngressMirrored:
                    description: |-
                      (Boolean) When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                      When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                    type: boolean
                  mirrorTxOnMirrorPort:
                    description: (Boolean)
                    type: boolean
                  mirroredPacketDropPrecedence:
                    description: |-
                      (String) Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                      Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                    type: string
                  mirroredPacketQosPriority:
                    description: |-
                      (Number) Remarked priority in mirrored packets.
                      Remarked priority in mirrored packets.
                    format: int64
                    type: integer
                  multicastLookupMode:
                    description: |-
                      mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                      Lookup mode for IPv4 multicast bridging.dst-mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                    type: string
                  name:
                    description: |-
                      (String) Name of the switch.
                      Name of the switch.
                    type: string
                  overrideExistingWhenUfdbFull:
                    description: |-
                      (Boolean) Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                      Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                    type: boolean
                  unicastFdbTimeout:
                    description: |-
                      (String) Timeout for Unicast FDB entries.
                      Timeout for Unicast FDB entries.
                    type: string
                  unknownVlanLookupMode:
                    description: |-
                      (String) Lookup and learning mode for packets with invalid VLAN.
                      Lookup and learning mode for packets with invalid VLAN.
                    type: string
                  useCvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  useSvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  vlanUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for VLAN-based mirroring.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: EthernetSwitchCrsStatus defines the observed state of EthernetSwitchCrs.
            properties:
              atProvider:
                properties:
                  bridgeType:
                    description: |-
                      VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                      The bridge type defines which VLAN tag is used as Lookup-VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.
                    type: string
                  bypassIngressPortPolicingFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress Port Policing. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassL2SecurityCheckFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Policy rule security check. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bypassVlanIngressFilterFor:
                    description: |-
                      discovery, ripv1).
                      Protocols that are excluded from Ingress VLAN filtering. These protocols are not dropped if they have invalid VLAN. (arp, dhcpv4, dhcpv6,eapol, igmp, mld, nd, pppoe-discovery, ripv1).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfInvalidOrSrcPortNotMemberOfVlanOnPorts:
                    description: |-
                      (Set of String) Ports that drop invalid and other port VLAN ID frames.
                      Ports that drop invalid and other port VLAN ID frames.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dropIfNoVlanAssignmentOnPorts:
                    description: |-
                      based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                      Ports which drop frames if no MAC-based, Protocol-based VLAN assignment or Ingress VLAN Translation is applied.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second egress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  egressMirrorRatio:
                    description: |-
                      (String) Proportion of egress mirrored packets compared to all packets.
                      Proportion of egress mirrored packets compared to all packets.
                    type: string
                  fdbUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for FDB-based mirroring.
                    type: string
                  forwardUnknownVlan:
                    description: |-
                      (Boolean) Whether to allow forwarding VLANs that are not members of the VLAN table.
                      Whether to allow forwarding VLANs that are not members of the VLAN table.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ingressMirror0:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The first ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirror1:
                    description: |-
                      configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                      The second ingress mirroring analyzer port or trunk and mirroring format:analyzer-configured - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the analyzer port.modified - The packet is the same as the packet to the destination. VLAN format is modified based on the VLAN configurations of the egress port.original - Traffic is mirrored without any change to the original incoming packet format. But the service VLAN tag is stripped in the edge port.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ingressMirrorRatio:
                    description: |-
                      (String) The proportion of ingress mirrored packets compared to all packets.
                      The proportion of ingress mirrored packets compared to all packets.
                    type: string
                  macLevelIsolation:
                    description: |-
                      profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                      Globally enables or disables MAC level isolation. Once enabled, the switch will check the source and destination MAC address entries and their isolation-profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is dropped when the source and destination MAC address isolation profile is isolated, or when the source and destination MAC address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is community2). When MAC level isolation is globally disabled, the isolation is bypassed.
                    type: boolean
                  mirrorEgressIfIngressMirrored:
                    description: |-
                      (Boolean) When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                      When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed on the packet, if this setting is disabled. If this setting is enabled both mirroring types are applied.
                    type: boolean
                  mirrorTxOnMirrorPort:
                    description: (Boolean)
                    type: boolean
                  mirroredPacketDropPrecedence:
                    description: |-
                      (String) Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                      Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet enqueuing or dropping.
                    type: string
                  mirroredPacketQosPriority:
                    description: |-
                      (Number) Remarked priority in mirrored packets.
                      Remarked priority in mirrored packets.
                    format: int64
                    type: integer
                  multicastLookupMode:
                    description: |-
                      mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                      Lookup mode for IPv4 multicast bridging.dst-mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key is the destination MAC and VLAN ID.
                    type: string
                  name:
                    description: |-
                      (String) Name of the switch.
                      Name of the switch.
                    type: string
                  overrideExistingWhenUfdbFull:
                    description: |-
                      (Boolean) Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                      Enable or disable to override existing entry which has the lowest aging value when UFDB is full.
                    type: boolean
                  type:
                    description: |-
                      chip type.
                      Switch-chip type.
                    type: string
                  unicastFdbTimeout:
                    description: |-
                      (String) Timeout for Unicast FDB entries.
                      Timeout for Unicast FDB entries.
                    type: string
                  unknownVlanLookupMode:
                    description: |-
                      (String) Lookup and learning mode for packets with invalid VLAN.
                      Lookup and learning mode for packets with invalid VLAN.
                    type: string
                  useCvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use customer VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  useSvidInOne2OneVlanLookup:
                    description: |-
                      (Boolean) Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                      Whether to use service VLAN ID for 1:1 VLAN switching lookup.
                    type: boolean
                  vlanUses:
                    description: |-
                      based mirroring.
                      Analyzer port used for VLAN-based mirroring.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
