---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: pppoeservers.interface.routeros.m.sindrip.io
spec:
  group: interface.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: PppoeServer
    listKind: PppoeServerList
    plural: pppoeservers
    singular: pppoeserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PppoeServer is the Schema for the PppoeServers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PppoeServerSpec defines the desired state of PppoeServer
            properties:
              forProvider:
                properties:
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  defaultProfile:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  interface:
                    description: |-
                      (String) Interface that the clients are connected to
                      Interface that the clients are connected to
                    type: string
                  keepaliveTimeout:
                    description: |-
                      timeout), the non responding client is proclaimed disconnected.
                      Defines the time period (in seconds) after which the router is starting to send keepalive packets every second. If there is no traffic and no keepalive responses arrive for that period of time (i.e. 2 * keepalive-timeout), the non responding client is proclaimed disconnected.
                    type: string
                  maxMru:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Receive Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Transmission Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxSessions:
                    description: |-
                      (Number) Maximum number of clients that the AC can serve. '0' = no limitations.
                      Maximum number of clients that the AC can serve. '0' = no limitations.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: (String)
                    type: string
                  oneSessionPerHost:
                    description: |-
                      (Boolean) Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                      Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                    type: boolean
                  pppoeOverVlanRange:
                    description: |-
                      service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                      This setting allows a PPPoE server to operate over 802.1Q VLANs. By default, a PPPoE server only accepts untagged packets on its interface. However, in scenarios where clients are on separate VLANs, instead of creating multiple 802.1Q VLAN interfaces and bridging them together or configuring individual PPPoE servers for each VLAN, you can specify the necessary VLANs directly in the PPPoE server settings. When you specify the VLAN IDs, the PPPoE server will accept both untagged packets and 802.1Q tagged packets from clients, and it will reply using the same VLAN. This setting can also be applied to both CVLAN and SVLAN interfaces. For example, when the use-service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                    format: int64
                    type: integer
                  service:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                  serviceName:
                    description: |-
                      names that matches this setting or if service-name field in PADI message is not set.
                      The PPPoE service name. Server will accept clients which sends PADI message with service-names that matches this setting or if service-name field in PADI message is not set.
                    type: string
                  user:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  defaultProfile:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  interface:
                    description: |-
                      (String) Interface that the clients are connected to
                      Interface that the clients are connected to
                    type: string
                  keepaliveTimeout:
                    description: |-
                      timeout), the non responding client is proclaimed disconnected.
                      Defines the time period (in seconds) after which the router is starting to send keepalive packets every second. If there is no traffic and no keepalive responses arrive for that period of time (i.e. 2 * keepalive-timeout), the non responding client is proclaimed disconnected.
                    type: string
                  maxMru:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Receive Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Transmission Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxSessions:
                    description: |-
                      (Number) Maximum number of clients that the AC can serve. '0' = no limitations.
                      Maximum number of clients that the AC can serve. '0' = no limitations.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: (String)
                    type: string
                  oneSessionPerHost:
                    description: |-
                      (Boolean) Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                      Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                    type: boolean
                  pppoeOverVlanRange:
                    description: |-
                      service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                      This setting allows a PPPoE server to operate over 802.1Q VLANs. By default, a PPPoE server only accepts untagged packets on its interface. However, in scenarios where clients are on separate VLANs, instead of creating multiple 802.1Q VLAN interfaces and bridging them together or configuring individual PPPoE servers for each VLAN, you can specify the necessary VLANs directly in the PPPoE server settings. When you specify the VLAN IDs, the PPPoE server will accept both untagged packets and 802.1Q tagged packets from clients, and it will reply using the same VLAN. This setting can also be applied to both CVLAN and SVLAN interfaces. For example, when the use-service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                    format: int64
                    type: integer
                  service:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                  serviceName:
                    description: |-
                      names that matches this setting or if service-name field in PADI message is not set.
                      The PPPoE service name. Server will accept clients which sends PADI message with service-names that matches this setting or if service-name field in PADI message is not set.
                    type: string
                  user:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PppoeServerStatus defines the observed state of PppoeServer.
            properties:
              atProvider:
                properties:
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  defaultProfile:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interface:
                    description: |-
                      (String) Interface that the clients are connected to
                      Interface that the clients are connected to
                    type: string
                  keepaliveTimeout:
                    description: |-
                      timeout), the non responding client is proclaimed disconnected.
                      Defines the time period (in seconds) after which the router is starting to send keepalive packets every second. If there is no traffic and no keepalive responses arrive for that period of time (i.e. 2 * keepalive-timeout), the non responding client is proclaimed disconnected.
                    type: string
                  maxMru:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Receive Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                      Maximum Transmission Unit. The optimal value is the MTU of the interface the tunnel is working over reduced by 20 (so, for 1500-byte Ethernet link, set the MTU to 1480 to avoid fragmentation of packets).
                    format: int64
                    type: integer
                  maxSessions:
                    description: |-
                      (Number) Maximum number of clients that the AC can serve. '0' = no limitations.
                      Maximum number of clients that the AC can serve. '0' = no limitations.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: (String)
                    type: string
                  oneSessionPerHost:
                    description: |-
                      (Boolean) Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                      Allow only one session per host (determined by MAC address). If a host tries to establish a new session, the old one will be closed.
                    type: boolean
                  pppoeOverVlanRange:
                    description: |-
                      service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                      This setting allows a PPPoE server to operate over 802.1Q VLANs. By default, a PPPoE server only accepts untagged packets on its interface. However, in scenarios where clients are on separate VLANs, instead of creating multiple 802.1Q VLAN interfaces and bridging them together or configuring individual PPPoE servers for each VLAN, you can specify the necessary VLANs directly in the PPPoE server settings. When you specify the VLAN IDs, the PPPoE server will accept both untagged packets and 802.1Q tagged packets from clients, and it will reply using the same VLAN. This setting can also be applied to both CVLAN and SVLAN interfaces. For example, when the use-service-tag=yes option is used on a VLAN interface, enabling QinQ setups as well. The setting supports a range of VLAN IDs, as well as individual VLANs specified using comma-separated values. For example: pppoe-over-vlan-range=100-115,120,122,128-130.
                    format: int64
                    type: integer
                  running:
                    description: (Boolean)
                    type: boolean
                  service:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                  serviceName:
                    description: |-
                      names that matches this setting or if service-name field in PADI message is not set.
                      The PPPoE service name. Server will accept clients which sends PADI message with service-names that matches this setting or if service-name field in PADI message is not set.
                    type: string
                  user:
                    description: |-
                      (String) This attribute is required in the ROS 7 version.
                      This attribute is required in the ROS 7 version.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
