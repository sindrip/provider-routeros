---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sstpclients.interface.routeros.m.sindrip.io
spec:
  group: interface.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: SstpClient
    listKind: SstpClientList
    plural: sstpclients
    singular: sstpclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'SstpClient is the Schema for the SstpClients API. <!-- schema
          generated by tfplugindocs --> ## Schema ### Required - `connect_to` (String)
          Remote address of the SSTP server. - `name` (String) Descriptive name of
          the interface. - `password` (String, Sensitive) Password used for authentication.
          - `user` (String) User name used for authentication. ### Optional - `add_default_route`
          (Boolean) Whether to add L2TP remote address as a default route. - `add_sni`
          (Boolean) Enables/disables service. - `authentication` (Set of String) Authentication
          algorithm. - `certificate` (String) Name of the certificate in use. - `ciphers`
          (String) Allowed ciphers. - `comment` (String) - `default_route_distance`
          (String) Sets distance value applied to auto created default route, if add-default-route
          is also selected. - `dial_on_demand` (Boolean) Connects only when outbound
          traffic is generated. If selected, then route with gateway address from
          10.112.112.0/24 network will be added while connection is not established.
          - `disabled` (Boolean) - `http_proxy` (String) Proxy address field. - `keepalive_timeout`
          (String) Sets keepalive timeout in seconds. - `max_mru` (Number) Maximum
          Receive Unit. - `max_mtu` (Number) Maximum Transmission Unit. - `mrru` (String)
          Maximum packet size that can be received on the link. If a packet is bigger
          than tunnel MTU, it will be split into multiple packets, allowing full size
          IP or Ethernet packets to be sent over the tunnel. - `pfs` (Boolean) Specifies
          which TLS authentication to use. With pfs=yes, TLS will use ECDHE-RSA- and
          DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
          - `port` (String) Sets port used. - `profile` (String) Specifies which PPP
          profile configuration will be used when establishing the tunnel. - `proxy_port`
          (String) Sets proxy port. - `tls_version` (String) Specifies which TLS versions
          to allow. - `verify_server_address_from_certificate` (Boolean) SSTP client
          will verify server address in certificate. - `verify_server_certificate`
          (Boolean) SSTP client will verify server certificate. ### Read-Only - `hw_crypto`
          (Boolean) - `id` (String) The ID of this resource. - `running` (Boolean)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SstpClientSpec defines the desired state of SstpClient
            properties:
              forProvider:
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add L2TP remote address as a default route.
                      Whether to add L2TP remote address as a default route.
                    type: boolean
                  addSni:
                    description: |-
                      (Boolean) Enables/disables service.
                      Enables/disables service.
                    type: boolean
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  certificate:
                    description: |-
                      (String) Name of the certificate in use.
                      Name of the certificate in use.
                    type: string
                  ciphers:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the SSTP server.
                      Remote address of the SSTP server.
                    type: string
                  defaultRouteDistance:
                    description: |-
                      default-route is also selected.
                      Sets distance value applied to auto created default route, if add-default-route is also selected.
                    type: string
                  dialOnDemand:
                    description: |-
                      (Boolean) Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                      Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  httpProxy:
                    description: |-
                      (String) Proxy address field.
                      Proxy address field.
                    type: string
                  keepaliveTimeout:
                    description: |-
                      (String) Sets keepalive timeout in seconds.
                      Sets keepalive timeout in seconds.
                    type: string
                  maxMru:
                    description: |-
                      (Number) Maximum Receive Unit.
                      Maximum Receive Unit.
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit.
                      Maximum Transmission Unit.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for authentication.
                      Password used for authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  pfs:
                    description: |-
                      RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                      Specifies which TLS authentication to use. With pfs=yes, TLS will use ECDHE-RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                    type: boolean
                  port:
                    description: |-
                      (String) Sets port used.
                      Sets port used.
                    type: string
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  proxyPort:
                    description: |-
                      (String) Sets proxy port.
                      Sets proxy port.
                    type: string
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerAddressFromCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server address in certificate.
                      SSTP client will verify server address in certificate.
                    type: boolean
                  verifyServerCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server certificate.
                      SSTP client will verify server certificate.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add L2TP remote address as a default route.
                      Whether to add L2TP remote address as a default route.
                    type: boolean
                  addSni:
                    description: |-
                      (Boolean) Enables/disables service.
                      Enables/disables service.
                    type: boolean
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  certificate:
                    description: |-
                      (String) Name of the certificate in use.
                      Name of the certificate in use.
                    type: string
                  ciphers:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the SSTP server.
                      Remote address of the SSTP server.
                    type: string
                  defaultRouteDistance:
                    description: |-
                      default-route is also selected.
                      Sets distance value applied to auto created default route, if add-default-route is also selected.
                    type: string
                  dialOnDemand:
                    description: |-
                      (Boolean) Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                      Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  httpProxy:
                    description: |-
                      (String) Proxy address field.
                      Proxy address field.
                    type: string
                  keepaliveTimeout:
                    description: |-
                      (String) Sets keepalive timeout in seconds.
                      Sets keepalive timeout in seconds.
                    type: string
                  maxMru:
                    description: |-
                      (Number) Maximum Receive Unit.
                      Maximum Receive Unit.
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit.
                      Maximum Transmission Unit.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for authentication.
                      Password used for authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  pfs:
                    description: |-
                      RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                      Specifies which TLS authentication to use. With pfs=yes, TLS will use ECDHE-RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                    type: boolean
                  port:
                    description: |-
                      (String) Sets port used.
                      Sets port used.
                    type: string
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  proxyPort:
                    description: |-
                      (String) Sets proxy port.
                      Sets proxy port.
                    type: string
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerAddressFromCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server address in certificate.
                      SSTP client will verify server address in certificate.
                    type: boolean
                  verifyServerCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server certificate.
                      SSTP client will verify server certificate.
                    type: boolean
                required:
                - passwordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.connectTo is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.connectTo)
                || (has(self.initProvider) && has(self.initProvider.connectTo))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.passwordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.passwordSecretRef)'
            - message: spec.forProvider.user is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.user)
                || (has(self.initProvider) && has(self.initProvider.user))'
          status:
            description: SstpClientStatus defines the observed state of SstpClient.
            properties:
              atProvider:
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add L2TP remote address as a default route.
                      Whether to add L2TP remote address as a default route.
                    type: boolean
                  addSni:
                    description: |-
                      (Boolean) Enables/disables service.
                      Enables/disables service.
                    type: boolean
                  authentication:
                    description: |-
                      (Set of String) Authentication algorithm.
                      Authentication algorithm.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  certificate:
                    description: |-
                      (String) Name of the certificate in use.
                      Name of the certificate in use.
                    type: string
                  ciphers:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the SSTP server.
                      Remote address of the SSTP server.
                    type: string
                  defaultRouteDistance:
                    description: |-
                      default-route is also selected.
                      Sets distance value applied to auto created default route, if add-default-route is also selected.
                    type: string
                  dialOnDemand:
                    description: |-
                      (Boolean) Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                      Connects only when outbound traffic is generated. If selected, then route with gateway address from 10.112.112.0/24 network will be added while connection is not established.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  httpProxy:
                    description: |-
                      (String) Proxy address field.
                      Proxy address field.
                    type: string
                  hwCrypto:
                    description: (Boolean)
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  keepaliveTimeout:
                    description: |-
                      (String) Sets keepalive timeout in seconds.
                      Sets keepalive timeout in seconds.
                    type: string
                  maxMru:
                    description: |-
                      (Number) Maximum Receive Unit.
                      Maximum Receive Unit.
                    format: int64
                    type: integer
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit.
                      Maximum Transmission Unit.
                    format: int64
                    type: integer
                  mrru:
                    description: |-
                      (String) Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                      Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  pfs:
                    description: |-
                      RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                      Specifies which TLS authentication to use. With pfs=yes, TLS will use ECDHE-RSA- and DHE-RSA-. For maximum security setting pfs=required will use only ECDHE.
                    type: boolean
                  port:
                    description: |-
                      (String) Sets port used.
                      Sets port used.
                    type: string
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  proxyPort:
                    description: |-
                      (String) Sets proxy port.
                      Sets proxy port.
                    type: string
                  running:
                    description: (Boolean)
                    type: boolean
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerAddressFromCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server address in certificate.
                      SSTP client will verify server address in certificate.
                    type: boolean
                  verifyServerCertificate:
                    description: |-
                      (Boolean) SSTP client will verify server certificate.
                      SSTP client will verify server certificate.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
