---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: w60gs.interface.routeros.m.sindrip.io
spec:
  group: interface.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: W60G
    listKind: W60GList
    plural: w60gs
    singular: w60g
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: W60G is the Schema for the W60Gs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: W60GSpec defines the desired state of W60G
            properties:
              forProvider:
                properties:
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  frequency:
                    description: |-
                      (String) Frequency used in communication (Only active on bridge device).
                      Frequency used in communication (Only active on bridge device).
                    type: string
                  isolateStations:
                    description: |-
                      (Boolean) Don't allow communication between connected clients (from RouterOS 6.41).
                      Don't allow communication between connected clients (from RouterOS 6.41).
                    type: boolean
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  macAddress:
                    description: |-
                      (String) MAC address of the radio interface.
                      MAC address of the radio interface.
                    type: string
                  mdmgFix:
                    description: |-
                      (Boolean) Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                      Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                    type: boolean
                  mode:
                    description: |-
                      (String) Operation mode.
                      Operation mode.
                    type: string
                  mtu:
                    description: |-
                      (String) Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                      Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                    type: string
                  name:
                    description: |-
                      (String) Name of the interface.
                      Name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for AES encryption.
                      Password used for AES encryption.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  putStationsInBridge:
                    description: |-
                      (String) Put newly created station device interfaces in this bridge.
                      Put newly created station device interfaces in this bridge.
                    type: string
                  region:
                    description: |-
                      (String) Parameter to limit frequency use.
                      Parameter to limit frequency use.
                    type: string
                  scanList:
                    description: |-
                      (Set of String) Scan list to limit connectivity over frequencies in station mode.
                      Scan list to limit connectivity over frequencies in station mode.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                      SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                    type: string
                  txSector:
                    description: |-
                      (Number) Disables beamforming and locks to selected radiation pattern.
                      Disables beamforming and locks to selected radiation pattern.
                    format: int64
                    type: integer
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  frequency:
                    description: |-
                      (String) Frequency used in communication (Only active on bridge device).
                      Frequency used in communication (Only active on bridge device).
                    type: string
                  isolateStations:
                    description: |-
                      (Boolean) Don't allow communication between connected clients (from RouterOS 6.41).
                      Don't allow communication between connected clients (from RouterOS 6.41).
                    type: boolean
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  macAddress:
                    description: |-
                      (String) MAC address of the radio interface.
                      MAC address of the radio interface.
                    type: string
                  mdmgFix:
                    description: |-
                      (Boolean) Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                      Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                    type: boolean
                  mode:
                    description: |-
                      (String) Operation mode.
                      Operation mode.
                    type: string
                  mtu:
                    description: |-
                      (String) Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                      Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                    type: string
                  name:
                    description: |-
                      (String) Name of the interface.
                      Name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for AES encryption.
                      Password used for AES encryption.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  putStationsInBridge:
                    description: |-
                      (String) Put newly created station device interfaces in this bridge.
                      Put newly created station device interfaces in this bridge.
                    type: string
                  region:
                    description: |-
                      (String) Parameter to limit frequency use.
                      Parameter to limit frequency use.
                    type: string
                  scanList:
                    description: |-
                      (Set of String) Scan list to limit connectivity over frequencies in station mode.
                      Scan list to limit connectivity over frequencies in station mode.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                      SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                    type: string
                  txSector:
                    description: |-
                      (Number) Disables beamforming and locks to selected radiation pattern.
                      Disables beamforming and locks to selected radiation pattern.
                    format: int64
                    type: integer
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: W60GStatus defines the observed state of W60G.
            properties:
              atProvider:
                properties:
                  arp:
                    description: |-
                      (String) Address Resolution Protocol mode:
                      Address Resolution Protocol mode:
                      * disabled - the interface will not use ARP
                      * enabled - the interface will use ARP
                      * local-proxy-arp - the router performs proxy ARP on the interface and sends replies to the same interface
                      * proxy-arp - the router performs proxy ARP on the interface and sends replies to other interfaces
                      * reply-only - the interface will only reply to requests originated from matching IP address/MAC address combinations which are entered as static entries in the ARP table. No dynamic entries will be automatically stored in the ARP table. Therefore for communications to be successful, a valid static entry must already exist.
                    type: string
                  arpTimeout:
                    description: |-
                      timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                      ARP timeout is time how long ARP record is kept in ARP table after no packets are received from IP. Value auto equals to the value of arp-timeout in IP/Settings, default is 30s. Can use postfix `ms`, `s`, `m`, `h`, `d` for milliseconds, seconds, minutes, hours or days. If no postfix is set then seconds (s) is used.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  frequency:
                    description: |-
                      (String) Frequency used in communication (Only active on bridge device).
                      Frequency used in communication (Only active on bridge device).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isolateStations:
                    description: |-
                      (Boolean) Don't allow communication between connected clients (from RouterOS 6.41).
                      Don't allow communication between connected clients (from RouterOS 6.41).
                    type: boolean
                  l2mtu:
                    description: |-
                      (Number) Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                      Layer2 Maximum transmission unit. [See](https://wiki.mikrotik.com/wiki/Maximum_Transmission_Unit_on_RouterBoards).
                    format: int64
                    type: integer
                  macAddress:
                    description: |-
                      (String) MAC address of the radio interface.
                      MAC address of the radio interface.
                    type: string
                  mdmgFix:
                    description: |-
                      (Boolean) Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                      Experimental feature working only on wAP60Gx3 devices, providing better point to multi point stability in some cases.
                    type: boolean
                  mode:
                    description: |-
                      (String) Operation mode.
                      Operation mode.
                    type: string
                  mtu:
                    description: |-
                      (String) Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                      Layer3 Maximum transmission unit ('auto', 0 .. 65535). Look for the exact minimum value in the MikroTik documentation
                    type: string
                  name:
                    description: |-
                      (String) Name of the interface.
                      Name of the interface.
                    type: string
                  putStationsInBridge:
                    description: |-
                      (String) Put newly created station device interfaces in this bridge.
                      Put newly created station device interfaces in this bridge.
                    type: string
                  region:
                    description: |-
                      (String) Parameter to limit frequency use.
                      Parameter to limit frequency use.
                    type: string
                  running:
                    description: (Boolean)
                    type: boolean
                  scanList:
                    description: |-
                      (Set of String) Scan list to limit connectivity over frequencies in station mode.
                      Scan list to limit connectivity over frequencies in station mode.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                      SSID (service set identifier) is a name that identifies wireless network (0..32 char).
                    type: string
                  txSector:
                    description: |-
                      (Number) Disables beamforming and locks to selected radiation pattern.
                      Disables beamforming and locks to selected radiation pattern.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
