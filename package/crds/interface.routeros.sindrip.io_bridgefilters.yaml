---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bridgefilters.interface.routeros.sindrip.io
spec:
  group: interface.routeros.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: BridgeFilter
    listKind: BridgeFilterList
    plural: bridgefilters
    singular: bridgefilter
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BridgeFilter is the Schema for the BridgeFilters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BridgeFilterSpec defines the desired state of BridgeFilter
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  action:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpDstMacAddress:
                    description: |-
                      (String) ARP destination MAC address
                      ARP destination MAC address
                    type: string
                  arpGratuitous:
                    description: |-
                      (Boolean) Matches ARP gratuitous packets.
                      Matches ARP gratuitous packets.
                    type: boolean
                  arpHardwareType:
                    description: |-
                      (Number) ARP hardware type. This is normally Ethernet (Type 1).
                      ARP hardware type. This is normally Ethernet (Type 1).
                    format: int64
                    type: integer
                  arpOpcode:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpPacketType:
                    description: |-
                      (Number) ARP Packet Type
                      ARP Packet Type
                    format: int64
                    type: integer
                  arpSrcAddress:
                    description: |-
                      (String) ARP source IP address.
                      ARP source IP address.
                    type: string
                  arpSrcMacAddress:
                    description: |-
                      (String) ARP source MAC address.
                      ARP source MAC address.
                    type: string
                  chain:
                    description: |-
                      (String) Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                      Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dstAddress:
                    description: |-
                      (String) Destination IP address (only if MAC protocol is set to IP).
                      Destination IP address (only if MAC protocol is set to IP).
                    type: string
                  dstMacAddress:
                    description: |-
                      (String) Destination MAC address.
                      Destination MAC address.
                    type: string
                  dstPort:
                    description: |-
                      (String) List of destination port numbers or port number ranges.
                      List of destination port numbers or port number ranges.
                    type: string
                  inBridge:
                    description: |-
                      (String) Bridge interface through which the packet is coming in.
                      Bridge interface through which the packet is coming in.
                    type: string
                  inBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as in-bridge.
                    type: string
                  inInterface:
                    description: |-
                      (String) Physical interface (i.e., bridge port) through which the packet is coming in.
                      Physical interface (i.e., bridge port) through which the packet is coming in.
                    type: string
                  inInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as in-interface.
                    type: string
                  ingressPriority:
                    description: |-
                      (Number) Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                      Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                    format: int64
                    type: integer
                  ipProtocol:
                    description: |-
                      (String) IP protocol (only if MAC protocol is set to IPv4)
                      IP protocol (only if MAC protocol is set to IPv4)
                    type: string
                  jumpTarget:
                    description: |-
                      (String) Name of the target chain to jump to. Applicable only if action=jump.
                      Name of the target chain to jump to. Applicable only if action=jump.
                    type: string
                  limit:
                    description: |-
                      (String) Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                      Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                    type: string
                  log:
                    description: |-
                      (Boolean) Add a message to the system log.
                      Add a message to the system log.
                    type: boolean
                  logPrefix:
                    description: |-
                      (String) Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                      Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                    type: string
                  macProtocol:
                    description: |-
                      level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                      Ethernet payload type (MAC-level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                    type: string
                  newPacketMark:
                    description: |-
                      mark value.
                      Sets a new packet-mark value.
                    type: string
                  newPriority:
                    description: |-
                      (Number) Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                      Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                    format: int64
                    type: integer
                  outBridge:
                    description: |-
                      (String) Bridge interface through which the packet going out.
                      Bridge interface through which the packet going out.
                    type: string
                  outBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as out-bridge.
                    type: string
                  outInterface:
                    description: |-
                      (String) Interface the packet has entered the router.
                      Interface the packet has entered the router.
                    type: string
                  outInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as out-interface.
                    type: string
                  packetMark:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  packetType:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  passthrough:
                    description: |-
                      (Boolean) Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                      Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                    type: boolean
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source port number or range (only for TCP or UDP protocols).
                      Source port number or range (only for TCP or UDP protocols).
                    type: string
                  srcMacAddress:
                    description: |-
                      (String) Source MAC address.
                      Source MAC address.
                    type: string
                  srcPort:
                    description: |-
                      (String) List of source port numbers or port number ranges.
                      List of source port numbers or port number ranges.
                    type: string
                  stpFlags:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  stpForwardDelay:
                    description: |-
                      (Number) Forward delay timer.
                      Forward delay timer.
                    format: int64
                    type: integer
                  stpHelloTime:
                    description: |-
                      (Number) STP hello packets time.
                      STP hello packets time.
                    format: int64
                    type: integer
                  stpMaxAge:
                    description: |-
                      (Number) Maximal STP message age.
                      Maximal STP message age.
                    format: int64
                    type: integer
                  stpPort:
                    description: |-
                      (Number) STP port identifier.
                      STP port identifier.
                    format: int64
                    type: integer
                  stpRootAddress:
                    description: |-
                      (String) Root bridge MAC address
                      Root bridge MAC address
                    type: string
                  stpRootCost:
                    description: |-
                      (Number) Root bridge cost.
                      Root bridge cost.
                    format: int64
                    type: integer
                  stpRootPriority:
                    description: (Number)
                    format: int64
                    type: integer
                  stpSenderAddress:
                    description: |-
                      (String) STP message sender MAC address.
                      STP message sender MAC address.
                    type: string
                  stpSenderPriority:
                    description: |-
                      (Number) STP sender priority.
                      STP sender priority.
                    format: int64
                    type: integer
                  stpType:
                    description: |-
                      configuration BPDU OR tcn - topology change notification
                      The BPDU type: config - configuration BPDU OR tcn - topology change notification
                    type: string
                  tlsHost:
                    description: |-
                      (String) Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                      Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                    type: string
                  vlanEncap:
                    description: |-
                      (Number) Matches the MAC protocol type encapsulated in the VLAN frame.
                      Matches the MAC protocol type encapsulated in the VLAN frame.
                    format: int64
                    type: integer
                  vlanId:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                  vlanPriority:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpDstMacAddress:
                    description: |-
                      (String) ARP destination MAC address
                      ARP destination MAC address
                    type: string
                  arpGratuitous:
                    description: |-
                      (Boolean) Matches ARP gratuitous packets.
                      Matches ARP gratuitous packets.
                    type: boolean
                  arpHardwareType:
                    description: |-
                      (Number) ARP hardware type. This is normally Ethernet (Type 1).
                      ARP hardware type. This is normally Ethernet (Type 1).
                    format: int64
                    type: integer
                  arpOpcode:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpPacketType:
                    description: |-
                      (Number) ARP Packet Type
                      ARP Packet Type
                    format: int64
                    type: integer
                  arpSrcAddress:
                    description: |-
                      (String) ARP source IP address.
                      ARP source IP address.
                    type: string
                  arpSrcMacAddress:
                    description: |-
                      (String) ARP source MAC address.
                      ARP source MAC address.
                    type: string
                  chain:
                    description: |-
                      (String) Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                      Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dstAddress:
                    description: |-
                      (String) Destination IP address (only if MAC protocol is set to IP).
                      Destination IP address (only if MAC protocol is set to IP).
                    type: string
                  dstMacAddress:
                    description: |-
                      (String) Destination MAC address.
                      Destination MAC address.
                    type: string
                  dstPort:
                    description: |-
                      (String) List of destination port numbers or port number ranges.
                      List of destination port numbers or port number ranges.
                    type: string
                  inBridge:
                    description: |-
                      (String) Bridge interface through which the packet is coming in.
                      Bridge interface through which the packet is coming in.
                    type: string
                  inBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as in-bridge.
                    type: string
                  inInterface:
                    description: |-
                      (String) Physical interface (i.e., bridge port) through which the packet is coming in.
                      Physical interface (i.e., bridge port) through which the packet is coming in.
                    type: string
                  inInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as in-interface.
                    type: string
                  ingressPriority:
                    description: |-
                      (Number) Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                      Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                    format: int64
                    type: integer
                  ipProtocol:
                    description: |-
                      (String) IP protocol (only if MAC protocol is set to IPv4)
                      IP protocol (only if MAC protocol is set to IPv4)
                    type: string
                  jumpTarget:
                    description: |-
                      (String) Name of the target chain to jump to. Applicable only if action=jump.
                      Name of the target chain to jump to. Applicable only if action=jump.
                    type: string
                  limit:
                    description: |-
                      (String) Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                      Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                    type: string
                  log:
                    description: |-
                      (Boolean) Add a message to the system log.
                      Add a message to the system log.
                    type: boolean
                  logPrefix:
                    description: |-
                      (String) Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                      Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                    type: string
                  macProtocol:
                    description: |-
                      level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                      Ethernet payload type (MAC-level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                    type: string
                  newPacketMark:
                    description: |-
                      mark value.
                      Sets a new packet-mark value.
                    type: string
                  newPriority:
                    description: |-
                      (Number) Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                      Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                    format: int64
                    type: integer
                  outBridge:
                    description: |-
                      (String) Bridge interface through which the packet going out.
                      Bridge interface through which the packet going out.
                    type: string
                  outBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as out-bridge.
                    type: string
                  outInterface:
                    description: |-
                      (String) Interface the packet has entered the router.
                      Interface the packet has entered the router.
                    type: string
                  outInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as out-interface.
                    type: string
                  packetMark:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  packetType:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  passthrough:
                    description: |-
                      (Boolean) Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                      Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                    type: boolean
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source port number or range (only for TCP or UDP protocols).
                      Source port number or range (only for TCP or UDP protocols).
                    type: string
                  srcMacAddress:
                    description: |-
                      (String) Source MAC address.
                      Source MAC address.
                    type: string
                  srcPort:
                    description: |-
                      (String) List of source port numbers or port number ranges.
                      List of source port numbers or port number ranges.
                    type: string
                  stpFlags:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  stpForwardDelay:
                    description: |-
                      (Number) Forward delay timer.
                      Forward delay timer.
                    format: int64
                    type: integer
                  stpHelloTime:
                    description: |-
                      (Number) STP hello packets time.
                      STP hello packets time.
                    format: int64
                    type: integer
                  stpMaxAge:
                    description: |-
                      (Number) Maximal STP message age.
                      Maximal STP message age.
                    format: int64
                    type: integer
                  stpPort:
                    description: |-
                      (Number) STP port identifier.
                      STP port identifier.
                    format: int64
                    type: integer
                  stpRootAddress:
                    description: |-
                      (String) Root bridge MAC address
                      Root bridge MAC address
                    type: string
                  stpRootCost:
                    description: |-
                      (Number) Root bridge cost.
                      Root bridge cost.
                    format: int64
                    type: integer
                  stpRootPriority:
                    description: (Number)
                    format: int64
                    type: integer
                  stpSenderAddress:
                    description: |-
                      (String) STP message sender MAC address.
                      STP message sender MAC address.
                    type: string
                  stpSenderPriority:
                    description: |-
                      (Number) STP sender priority.
                      STP sender priority.
                    format: int64
                    type: integer
                  stpType:
                    description: |-
                      configuration BPDU OR tcn - topology change notification
                      The BPDU type: config - configuration BPDU OR tcn - topology change notification
                    type: string
                  tlsHost:
                    description: |-
                      (String) Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                      Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                    type: string
                  vlanEncap:
                    description: |-
                      (Number) Matches the MAC protocol type encapsulated in the VLAN frame.
                      Matches the MAC protocol type encapsulated in the VLAN frame.
                    format: int64
                    type: integer
                  vlanId:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                  vlanPriority:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.chain is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.chain)
                || (has(self.initProvider) && has(self.initProvider.chain))'
          status:
            description: BridgeFilterStatus defines the observed state of BridgeFilter.
            properties:
              atProvider:
                properties:
                  action:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpDstMacAddress:
                    description: |-
                      (String) ARP destination MAC address
                      ARP destination MAC address
                    type: string
                  arpGratuitous:
                    description: |-
                      (Boolean) Matches ARP gratuitous packets.
                      Matches ARP gratuitous packets.
                    type: boolean
                  arpHardwareType:
                    description: |-
                      (Number) ARP hardware type. This is normally Ethernet (Type 1).
                      ARP hardware type. This is normally Ethernet (Type 1).
                    format: int64
                    type: integer
                  arpOpcode:
                    description: |-
                      (String) Action to take if a packet is matched by the rule
                      Action to take if a packet is matched by the rule
                    type: string
                  arpPacketType:
                    description: |-
                      (Number) ARP Packet Type
                      ARP Packet Type
                    format: int64
                    type: integer
                  arpSrcAddress:
                    description: |-
                      (String) ARP source IP address.
                      ARP source IP address.
                    type: string
                  arpSrcMacAddress:
                    description: |-
                      (String) ARP source MAC address.
                      ARP source MAC address.
                    type: string
                  chain:
                    description: |-
                      (String) Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                      Specifies to which chain rule will be added. If the input does not match the name of an already defined chain, a new chain will be created.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dstAddress:
                    description: |-
                      (String) Destination IP address (only if MAC protocol is set to IP).
                      Destination IP address (only if MAC protocol is set to IP).
                    type: string
                  dstMacAddress:
                    description: |-
                      (String) Destination MAC address.
                      Destination MAC address.
                    type: string
                  dstPort:
                    description: |-
                      (String) List of destination port numbers or port number ranges.
                      List of destination port numbers or port number ranges.
                    type: string
                  dynamic:
                    description: |-
                      (Boolean) Configuration item created by software, not by management interface. It is not exported, and cannot be directly modified.
                      Configuration item created by software, not by management interface. It is not exported, and cannot be directly modified.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  inBridge:
                    description: |-
                      (String) Bridge interface through which the packet is coming in.
                      Bridge interface through which the packet is coming in.
                    type: string
                  inBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as in-bridge.
                    type: string
                  inInterface:
                    description: |-
                      (String) Physical interface (i.e., bridge port) through which the packet is coming in.
                      Physical interface (i.e., bridge port) through which the packet is coming in.
                    type: string
                  inInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as in-interface.
                    type: string
                  ingressPriority:
                    description: |-
                      (Number) Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                      Integer. Matches the priority of an ingress packet. Priority may be derived from VLAN, WMM, DSCP,or MPLS EXP bit.
                    format: int64
                    type: integer
                  ipProtocol:
                    description: |-
                      (String) IP protocol (only if MAC protocol is set to IPv4)
                      IP protocol (only if MAC protocol is set to IPv4)
                    type: string
                  jumpTarget:
                    description: |-
                      (String) Name of the target chain to jump to. Applicable only if action=jump.
                      Name of the target chain to jump to. Applicable only if action=jump.
                    type: string
                  limit:
                    description: |-
                      (String) Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                      Matches packets up to a limited rate (packet rate or bit rate). A rule using this matcher will match until this limit is reached. Parameters are written in the following format: rate[/time],burst:mode.
                    type: string
                  log:
                    description: |-
                      (Boolean) Add a message to the system log.
                      Add a message to the system log.
                    type: boolean
                  logPrefix:
                    description: |-
                      (String) Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                      Adds specified text at the beginning of every log message. Applicable if action=log or log=yes configured.
                    type: string
                  macProtocol:
                    description: |-
                      level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                      Ethernet payload type (MAC-level protocol). To match protocol type for VLAN encapsulated frames (0x8100 or 0x88a8), a vlan-encap property should be used.
                    type: string
                  newPacketMark:
                    description: |-
                      mark value.
                      Sets a new packet-mark value.
                    type: string
                  newPriority:
                    description: |-
                      (Number) Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                      Sets a new priority for a packet. This can be the VLAN, WMM or MPLS EXP priority
                    format: int64
                    type: integer
                  outBridge:
                    description: |-
                      (String) Bridge interface through which the packet going out.
                      Bridge interface through which the packet going out.
                    type: string
                  outBridgeList:
                    description: |-
                      bridge.
                      Set of bridge interfaces defined in interface list. Works the same as out-bridge.
                    type: string
                  outInterface:
                    description: |-
                      (String) Interface the packet has entered the router.
                      Interface the packet has entered the router.
                    type: string
                  outInterfaceList:
                    description: |-
                      interface.
                      Set of interfaces defined in interface list. Works the same as out-interface.
                    type: string
                  packetMark:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  packetType:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  passthrough:
                    description: |-
                      (Boolean) Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                      Whether to let the packet to pass further (like action passthrough) into the filter or not (property only valid some actions).
                    type: boolean
                  placeBefore:
                    description: |-
                      (String) Before which position the rule will be inserted.
                      Before which position the rule will be inserted.
                      > Please check the effect of this option, as it does not work as you think!
                      > Best way to use in conjunction with a data source. See [example](../data-sources/ip_firewall.md#example-usage).
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source port number or range (only for TCP or UDP protocols).
                      Source port number or range (only for TCP or UDP protocols).
                    type: string
                  srcMacAddress:
                    description: |-
                      (String) Source MAC address.
                      Source MAC address.
                    type: string
                  srcPort:
                    description: |-
                      (String) List of source port numbers or port number ranges.
                      List of source port numbers or port number ranges.
                    type: string
                  stpFlags:
                    description: |-
                      (String) Match packets with a certain packet mark.
                      Match packets with a certain packet mark.
                    type: string
                  stpForwardDelay:
                    description: |-
                      (Number) Forward delay timer.
                      Forward delay timer.
                    format: int64
                    type: integer
                  stpHelloTime:
                    description: |-
                      (Number) STP hello packets time.
                      STP hello packets time.
                    format: int64
                    type: integer
                  stpMaxAge:
                    description: |-
                      (Number) Maximal STP message age.
                      Maximal STP message age.
                    format: int64
                    type: integer
                  stpPort:
                    description: |-
                      (Number) STP port identifier.
                      STP port identifier.
                    format: int64
                    type: integer
                  stpRootAddress:
                    description: |-
                      (String) Root bridge MAC address
                      Root bridge MAC address
                    type: string
                  stpRootCost:
                    description: |-
                      (Number) Root bridge cost.
                      Root bridge cost.
                    format: int64
                    type: integer
                  stpRootPriority:
                    description: (Number)
                    format: int64
                    type: integer
                  stpSenderAddress:
                    description: |-
                      (String) STP message sender MAC address.
                      STP message sender MAC address.
                    type: string
                  stpSenderPriority:
                    description: |-
                      (Number) STP sender priority.
                      STP sender priority.
                    format: int64
                    type: integer
                  stpType:
                    description: |-
                      configuration BPDU OR tcn - topology change notification
                      The BPDU type: config - configuration BPDU OR tcn - topology change notification
                    type: string
                  tlsHost:
                    description: |-
                      (String) Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                      Allows matching https traffic based on TLS SNI hostname. Accepts GLOB syntax for wildcard matching
                    type: string
                  vlanEncap:
                    description: |-
                      (Number) Matches the MAC protocol type encapsulated in the VLAN frame.
                      Matches the MAC protocol type encapsulated in the VLAN frame.
                    format: int64
                    type: integer
                  vlanId:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                  vlanPriority:
                    description: |-
                      (Number) Matches the VLAN identifier field.
                      Matches the VLAN identifier field.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
