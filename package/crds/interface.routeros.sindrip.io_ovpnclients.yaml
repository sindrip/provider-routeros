---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ovpnclients.interface.routeros.sindrip.io
spec:
  group: interface.routeros.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: OvpnClient
    listKind: OvpnClientList
    plural: ovpnclients
    singular: ovpnclient
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'OvpnClient is the Schema for the OvpnClients API. <!-- schema
          generated by tfplugindocs --> ## Schema ### Required - `connect_to` (String)
          Remote address of the OVPN server. - `name` (String) Descriptive name of
          the interface. - `user` (String) User name used for authentication. ###
          Optional - `add_default_route` (Boolean) Whether to add OVPN remote address
          as a default route. - `auth` (String) Authentication methods that the server
          will accept. - `certificate` (String) Name of the client certificate. -
          `cipher` (String) Allowed ciphers. - `comment` (String) - `disabled` (Boolean)
          - `mac_address` (String) Mac address of OVPN interface. Will be automatically
          generated if not specified. - `max_mtu` (Number) Maximum Transmission Unit.
          Max packet size that the OVPN interface will be able to send without packet
          fragmentation. - `mode` (String) Layer3 or layer2 tunnel mode (alternatively
          tun, tap) - `password` (String, Sensitive) Password used for authentication.
          - `port` (Number) Port to connect to. - `profile` (String) Specifies which
          PPP profile configuration will be used when establishing the tunnel. - `protocol`
          (String) Indicates the protocol to use when connecting with the remote endpoint.
          - `route_nopull` (Boolean) Specifies whether to allow the OVPN server to
          add routes to the OVPN client instance routing table. - `tls_version` (String)
          Specifies which TLS versions to allow. - `use_peer_dns` (Boolean) Whether
          to add DNS servers provided by the OVPN server to IP/DNS configuration.
          - `verify_server_certificate` (Boolean) Checks the certificates CN or SAN
          against the "connect-to" parameter. The IP or hostname must be present in
          the server''s certificate. ### Read-Only - `hw_crypto` (Boolean) - `id`
          (String) The ID of this resource. - `running` (Boolean)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OvpnClientSpec defines the desired state of OvpnClient
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add OVPN remote address as a default route.
                      Whether to add OVPN remote address as a default route.
                    type: boolean
                  auth:
                    description: |-
                      (String) Authentication methods that the server will accept.
                      Authentication methods that the server will accept.
                    type: string
                  certificate:
                    description: |-
                      (String) Name of the client certificate.
                      Name of the client certificate.
                    type: string
                  cipher:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the OVPN server.
                      Remote address of the OVPN server.
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  macAddress:
                    description: |-
                      (String) Mac address of OVPN interface. Will be automatically generated if not specified.
                      Mac address of OVPN interface. Will be automatically generated if not specified.
                    type: string
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                      Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                    format: int64
                    type: integer
                  mode:
                    description: |-
                      (String) Layer3 or layer2 tunnel mode (alternatively tun, tap)
                      Layer3 or layer2 tunnel mode (alternatively tun, tap)
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for authentication.
                      Password used for authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  port:
                    description: |-
                      (Number) Port to connect to.
                      Port to connect to.
                    format: int64
                    type: integer
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  protocol:
                    description: |-
                      (String) Indicates the protocol to use when connecting with the remote endpoint.
                      Indicates the protocol to use when connecting with the remote endpoint.
                    type: string
                  routeNopull:
                    description: |-
                      (Boolean) Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                      Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                    type: boolean
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  usePeerDns:
                    description: |-
                      (Boolean) Whether to add DNS servers provided by the OVPN server to IP/DNS configuration.
                      Whether to add DNS servers provided by the OVPN server to IP/DNS configuration. RouterOS accepts "exclusively" (use only the peer's servers), "yes" or "no"; it rejects true and false.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerCertificate:
                    description: |-
                      to" parameter. The IP or hostname must be present in the server's certificate.
                      Checks the certificates CN or SAN against the "connect-to" parameter. The IP or hostname must be present in the server's certificate.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add OVPN remote address as a default route.
                      Whether to add OVPN remote address as a default route.
                    type: boolean
                  auth:
                    description: |-
                      (String) Authentication methods that the server will accept.
                      Authentication methods that the server will accept.
                    type: string
                  certificate:
                    description: |-
                      (String) Name of the client certificate.
                      Name of the client certificate.
                    type: string
                  cipher:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the OVPN server.
                      Remote address of the OVPN server.
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  macAddress:
                    description: |-
                      (String) Mac address of OVPN interface. Will be automatically generated if not specified.
                      Mac address of OVPN interface. Will be automatically generated if not specified.
                    type: string
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                      Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                    format: int64
                    type: integer
                  mode:
                    description: |-
                      (String) Layer3 or layer2 tunnel mode (alternatively tun, tap)
                      Layer3 or layer2 tunnel mode (alternatively tun, tap)
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) Password used for authentication.
                      Password used for authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  port:
                    description: |-
                      (Number) Port to connect to.
                      Port to connect to.
                    format: int64
                    type: integer
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  protocol:
                    description: |-
                      (String) Indicates the protocol to use when connecting with the remote endpoint.
                      Indicates the protocol to use when connecting with the remote endpoint.
                    type: string
                  routeNopull:
                    description: |-
                      (Boolean) Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                      Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                    type: boolean
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  usePeerDns:
                    description: |-
                      (Boolean) Whether to add DNS servers provided by the OVPN server to IP/DNS configuration.
                      Whether to add DNS servers provided by the OVPN server to IP/DNS configuration. RouterOS accepts "exclusively" (use only the peer's servers), "yes" or "no"; it rejects true and false.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerCertificate:
                    description: |-
                      to" parameter. The IP or hostname must be present in the server's certificate.
                      Checks the certificates CN or SAN against the "connect-to" parameter. The IP or hostname must be present in the server's certificate.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.connectTo is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.connectTo)
                || (has(self.initProvider) && has(self.initProvider.connectTo))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.user is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.user)
                || (has(self.initProvider) && has(self.initProvider.user))'
          status:
            description: OvpnClientStatus defines the observed state of OvpnClient.
            properties:
              atProvider:
                properties:
                  addDefaultRoute:
                    description: |-
                      (Boolean) Whether to add OVPN remote address as a default route.
                      Whether to add OVPN remote address as a default route.
                    type: boolean
                  auth:
                    description: |-
                      (String) Authentication methods that the server will accept.
                      Authentication methods that the server will accept.
                    type: string
                  certificate:
                    description: |-
                      (String) Name of the client certificate.
                      Name of the client certificate.
                    type: string
                  cipher:
                    description: |-
                      (String) Allowed ciphers.
                      Allowed ciphers.
                    type: string
                  comment:
                    description: (String)
                    type: string
                  connectTo:
                    description: |-
                      (String) Remote address of the OVPN server.
                      Remote address of the OVPN server.
                    type: string
                  disabled:
                    description: (Boolean)
                    type: boolean
                  hwCrypto:
                    description: (Boolean)
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  macAddress:
                    description: |-
                      (String) Mac address of OVPN interface. Will be automatically generated if not specified.
                      Mac address of OVPN interface. Will be automatically generated if not specified.
                    type: string
                  maxMtu:
                    description: |-
                      (Number) Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                      Maximum Transmission Unit. Max packet size that the OVPN interface will be able to send without packet fragmentation.
                    format: int64
                    type: integer
                  mode:
                    description: |-
                      (String) Layer3 or layer2 tunnel mode (alternatively tun, tap)
                      Layer3 or layer2 tunnel mode (alternatively tun, tap)
                    type: string
                  name:
                    description: |-
                      (String) Descriptive name of the interface.
                      Descriptive name of the interface.
                    type: string
                  port:
                    description: |-
                      (Number) Port to connect to.
                      Port to connect to.
                    format: int64
                    type: integer
                  profile:
                    description: |-
                      (String) Specifies which PPP profile configuration will be used when establishing the tunnel.
                      Specifies which PPP profile configuration will be used when establishing the tunnel.
                    type: string
                  protocol:
                    description: |-
                      (String) Indicates the protocol to use when connecting with the remote endpoint.
                      Indicates the protocol to use when connecting with the remote endpoint.
                    type: string
                  routeNopull:
                    description: |-
                      (Boolean) Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                      Specifies whether to allow the OVPN server to add routes to the OVPN client instance routing table.
                    type: boolean
                  running:
                    description: (Boolean)
                    type: boolean
                  tlsVersion:
                    description: |-
                      (String) Specifies which TLS versions to allow.
                      Specifies which TLS versions to allow.
                    type: string
                  usePeerDns:
                    description: |-
                      (Boolean) Whether to add DNS servers provided by the OVPN server to IP/DNS configuration.
                      Whether to add DNS servers provided by the OVPN server to IP/DNS configuration. RouterOS accepts "exclusively" (use only the peer's servers), "yes" or "no"; it rejects true and false.
                    type: string
                  user:
                    description: |-
                      (String) User name used for authentication.
                      User name used for authentication.
                    type: string
                  verifyServerCertificate:
                    description: |-
                      to" parameter. The IP or hostname must be present in the server's certificate.
                      Checks the certificates CN or SAN against the "connect-to" parameter. The IP or hostname must be present in the server's certificate.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
