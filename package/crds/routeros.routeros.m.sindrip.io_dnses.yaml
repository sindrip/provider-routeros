---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dnses.routeros.routeros.m.sindrip.io
spec:
  group: routeros.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: DNS
    listKind: DNSList
    plural: dnses
    singular: dns
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'DNS is the Schema for the DNSs API. #### This is an alias for
          backwards compatibility between plugin versions. Please see documentation
          for [routeros_ip_dns](ip_dns.md)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DNSSpec defines the desired state of DNS
            properties:
              forProvider:
                properties:
                  addressListExtraTime:
                    type: string
                  allowRemoteRequests:
                    description: Specifies whether to allow network requests.
                    type: boolean
                  cacheMaxTtl:
                    description: 'Maximum time-to-live for cache records. In other
                      words, cache records will expire unconditionally after cache-max-ttl
                      time. Shorter TTL received from DNS servers are respected. *Default:
                      1w*'
                    type: string
                  cacheSize:
                    description: 'Specifies the size of DNS cache in KiB (64..4294967295).
                      *Default: 2048*'
                    format: int64
                    type: integer
                  dohMaxConcurrentQueries:
                    description: Specifies how many DoH concurrent queries are allowed.
                    format: int64
                    type: integer
                  dohMaxServerConnections:
                    description: Specifies how many concurrent connections to the
                      DoH server are allowed.
                    format: int64
                    type: integer
                  dohTimeout:
                    description: Specifies how long to wait for query response from
                      the DoH server.
                    type: string
                  maxConcurrentQueries:
                    description: 'Specifies how much concurrent queries are allowed.
                      *Default: 100*'
                    format: int64
                    type: integer
                  maxConcurrentTcpSessions:
                    description: 'Specifies how much concurrent TCP sessions are allowed.
                      *Default: 20*'
                    format: int64
                    type: integer
                  maxUdpPacketSize:
                    description: 'Maximum size of allowed UDP packet. *Default: 4096*'
                    format: int64
                    type: integer
                  mdnsRepeatIfaces:
                    description: An option to enable mDNS repeater on specified interfaces.
                      This option is available in RouterOS starting from version 7.16.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  queryServerTimeout:
                    description: 'Specifies how long to wait for query response from
                      one server. Time can be specified in milliseconds. *Default:
                      2s*'
                    type: string
                  queryTotalTimeout:
                    description: 'Specifies how long to wait for query response in
                      total. Note that this setting must be configured taking into
                      account query_server_timeout and number of used DNS server.
                      Time can be specified in milliseconds. *Default: 10s*'
                    type: string
                  servers:
                    description: List of DNS server IPv4/IPv6 addresses.
                    items:
                      type: string
                    type: array
                  useDohServer:
                    description: |-
                      DNS over HTTPS (DoH) server URL.
                      > Mikrotik strongly suggest not use third-party download links for certificate fetching.
                      Use the Certificate Authority's own website.

                      > RouterOS prioritize DoH over DNS server if both are configured on the device.
                    type: string
                  verifyDohCert:
                    description: DoH certificate verification. [See docs](https://wiki.mikrotik.com/wiki/Manual:IP/DNS#DNS_over_HTTPS).
                    type: boolean
                  vrf:
                    description: The VRF table this resource operates on.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addressListExtraTime:
                    type: string
                  allowRemoteRequests:
                    description: Specifies whether to allow network requests.
                    type: boolean
                  cacheMaxTtl:
                    description: 'Maximum time-to-live for cache records. In other
                      words, cache records will expire unconditionally after cache-max-ttl
                      time. Shorter TTL received from DNS servers are respected. *Default:
                      1w*'
                    type: string
                  cacheSize:
                    description: 'Specifies the size of DNS cache in KiB (64..4294967295).
                      *Default: 2048*'
                    format: int64
                    type: integer
                  dohMaxConcurrentQueries:
                    description: Specifies how many DoH concurrent queries are allowed.
                    format: int64
                    type: integer
                  dohMaxServerConnections:
                    description: Specifies how many concurrent connections to the
                      DoH server are allowed.
                    format: int64
                    type: integer
                  dohTimeout:
                    description: Specifies how long to wait for query response from
                      the DoH server.
                    type: string
                  maxConcurrentQueries:
                    description: 'Specifies how much concurrent queries are allowed.
                      *Default: 100*'
                    format: int64
                    type: integer
                  maxConcurrentTcpSessions:
                    description: 'Specifies how much concurrent TCP sessions are allowed.
                      *Default: 20*'
                    format: int64
                    type: integer
                  maxUdpPacketSize:
                    description: 'Maximum size of allowed UDP packet. *Default: 4096*'
                    format: int64
                    type: integer
                  mdnsRepeatIfaces:
                    description: An option to enable mDNS repeater on specified interfaces.
                      This option is available in RouterOS starting from version 7.16.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  queryServerTimeout:
                    description: 'Specifies how long to wait for query response from
                      one server. Time can be specified in milliseconds. *Default:
                      2s*'
                    type: string
                  queryTotalTimeout:
                    description: 'Specifies how long to wait for query response in
                      total. Note that this setting must be configured taking into
                      account query_server_timeout and number of used DNS server.
                      Time can be specified in milliseconds. *Default: 10s*'
                    type: string
                  servers:
                    description: List of DNS server IPv4/IPv6 addresses.
                    items:
                      type: string
                    type: array
                  useDohServer:
                    description: |-
                      DNS over HTTPS (DoH) server URL.
                      > Mikrotik strongly suggest not use third-party download links for certificate fetching.
                      Use the Certificate Authority's own website.

                      > RouterOS prioritize DoH over DNS server if both are configured on the device.
                    type: string
                  verifyDohCert:
                    description: DoH certificate verification. [See docs](https://wiki.mikrotik.com/wiki/Manual:IP/DNS#DNS_over_HTTPS).
                    type: boolean
                  vrf:
                    description: The VRF table this resource operates on.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DNSStatus defines the observed state of DNS.
            properties:
              atProvider:
                properties:
                  addressListExtraTime:
                    type: string
                  allowRemoteRequests:
                    description: Specifies whether to allow network requests.
                    type: boolean
                  cacheMaxTtl:
                    description: 'Maximum time-to-live for cache records. In other
                      words, cache records will expire unconditionally after cache-max-ttl
                      time. Shorter TTL received from DNS servers are respected. *Default:
                      1w*'
                    type: string
                  cacheSize:
                    description: 'Specifies the size of DNS cache in KiB (64..4294967295).
                      *Default: 2048*'
                    format: int64
                    type: integer
                  cacheUsed:
                    description: Shows the currently used cache size in KiB.
                    format: int64
                    type: integer
                  dohMaxConcurrentQueries:
                    description: Specifies how many DoH concurrent queries are allowed.
                    format: int64
                    type: integer
                  dohMaxServerConnections:
                    description: Specifies how many concurrent connections to the
                      DoH server are allowed.
                    format: int64
                    type: integer
                  dohTimeout:
                    description: Specifies how long to wait for query response from
                      the DoH server.
                    type: string
                  dynamicServers:
                    description: List of dynamically added DNS server from different
                      services, for example, DHCP.
                    type: string
                  id:
                    type: string
                  maxConcurrentQueries:
                    description: 'Specifies how much concurrent queries are allowed.
                      *Default: 100*'
                    format: int64
                    type: integer
                  maxConcurrentTcpSessions:
                    description: 'Specifies how much concurrent TCP sessions are allowed.
                      *Default: 20*'
                    format: int64
                    type: integer
                  maxUdpPacketSize:
                    description: 'Maximum size of allowed UDP packet. *Default: 4096*'
                    format: int64
                    type: integer
                  mdnsRepeatIfaces:
                    description: An option to enable mDNS repeater on specified interfaces.
                      This option is available in RouterOS starting from version 7.16.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  queryServerTimeout:
                    description: 'Specifies how long to wait for query response from
                      one server. Time can be specified in milliseconds. *Default:
                      2s*'
                    type: string
                  queryTotalTimeout:
                    description: 'Specifies how long to wait for query response in
                      total. Note that this setting must be configured taking into
                      account query_server_timeout and number of used DNS server.
                      Time can be specified in milliseconds. *Default: 10s*'
                    type: string
                  servers:
                    description: List of DNS server IPv4/IPv6 addresses.
                    items:
                      type: string
                    type: array
                  useDohServer:
                    description: |-
                      DNS over HTTPS (DoH) server URL.
                      > Mikrotik strongly suggest not use third-party download links for certificate fetching.
                      Use the Certificate Authority's own website.

                      > RouterOS prioritize DoH over DNS server if both are configured on the device.
                    type: string
                  verifyDohCert:
                    description: DoH certificate verification. [See docs](https://wiki.mikrotik.com/wiki/Manual:IP/DNS#DNS_over_HTTPS).
                    type: boolean
                  vrf:
                    description: The VRF table this resource operates on.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
