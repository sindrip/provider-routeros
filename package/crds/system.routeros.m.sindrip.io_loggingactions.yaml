---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: loggingactions.system.routeros.m.sindrip.io
spec:
  group: system.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: LoggingAction
    listKind: LoggingActionList
    plural: loggingactions
    singular: loggingaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'LoggingAction is the Schema for the LoggingActions API. <!--
          schema generated by tfplugindocs --> ## Schema ### Required - `name` (String)
          Name of an action. - `target` (String) Storage facility or target of log
          messages. ### Optional - `bsd_syslog` (Boolean, Deprecated) Whether to use
          bsd-syslog as defined in RFC 3164. - `cef_event_delimiter` (String) Option
          helps remote syslog to distinguish between individual events within sent
          batch - `disk_file_count` (Number) Specifies number of files used to store
          log messages, applicable only if `action=disk`. - `disk_file_name` (String)
          Name of the file used to store log messages, applicable only if `action=disk`.
          - `disk_lines_per_file` (Number) Specifies maximum size of file in lines,
          applicable only if `action=disk`. - `disk_stop_on_full` (Boolean) Whether
          to stop to save log messages to disk after the specified disk-lines-per-file
          and disk-file-count number is reached, applicable only if `action=disk`.
          - `email_start_tls` (Boolean) Whether to use tls when sending email, applicable
          only if `action=email`. - `email_to` (String) Email address where logs are
          sent, applicable only if `action=email`. - `memory_lines` (Number) Number
          of records in local memory buffer, applicable only if `action=memory`. -
          `memory_stop_on_full` (Boolean) Whether to stop to save log messages in
          local buffer after the specified memory-lines number is reached. - `remember`
          (Boolean) Whether to keep log messages, which have not yet been displayed
          in console, applicable if `action=echo`. - `remote` (String) Remote logging
          server''s IP/IPv6 address, applicable if `action=remote`. - `remote_log_format`
          (String) Format for logs to be sent to remote instance: - **cef** - logs
          are sent in CEF format; - **default** - logs are sent as it is; - **syslog**
          - logs are sent in BSD-syslog format. - `remote_port` (Number) Remote logging
          server''s UDP port, applicable if `action=remote`. - `remote_protocol` (String)
          Protocol for remote logging messages. - `src_address` (String) Source address
          used when sending packets to remote server, applicable if `action=remote`.
          - `syslog_facility` (String) SYSLOG facility, applicable if `action=remote`.
          - `syslog_severity` (String) Severity level indicator defined in RFC 3164,
          applicable if `action=remote`. - `syslog_time_format` (String) SYSLOG time
          format (`bsd-syslog` or `iso8601`). - `vrf` (String) The VRF table this
          resource operates on. ### Read-Only - `default` (Boolean) It''s the default
          item. - `id` (String) The ID of this resource.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoggingActionSpec defines the desired state of LoggingAction
            properties:
              forProvider:
                properties:
                  bsdSyslog:
                    description: |-
                      syslog as defined in RFC 3164.
                      Whether to use bsd-syslog as defined in RFC 3164.
                    type: boolean
                  cefEventDelimiter:
                    description: |-
                      (String) Option helps remote syslog to distinguish between individual events within sent batch
                      Option helps remote syslog to distinguish between individual events within sent batch
                    type: string
                  diskFileCount:
                    description: |-
                      (Number) Specifies number of files used to store log messages, applicable only if `action=disk`.
                      Specifies number of files used to store log messages, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskFileName:
                    description: |-
                      (String) Name of the file used to store log messages, applicable only if `action=disk`.
                      Name of the file used to store log messages, applicable only if `action=disk`.
                    type: string
                  diskLinesPerFile:
                    description: |-
                      (Number) Specifies maximum size of file in lines, applicable only if `action=disk`.
                      Specifies maximum size of file in lines, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskStopOnFull:
                    description: |-
                      lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                      Whether to stop to save log messages to disk after the specified disk-lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                    type: boolean
                  emailStartTls:
                    description: |-
                      (Boolean) Whether to use tls when sending email, applicable only if `action=email`.
                      Whether to use tls when sending email, applicable only if `action=email`.
                    type: boolean
                  emailTo:
                    description: |-
                      (String) Email address where logs are sent, applicable only if `action=email`.
                      Email address where logs are sent, applicable only if `action=email`.
                    type: string
                  memoryLines:
                    description: |-
                      (Number) Number of records in local memory buffer, applicable only if `action=memory`.
                      Number of records in local memory buffer, applicable only if `action=memory`.
                    format: int64
                    type: integer
                  memoryStopOnFull:
                    description: |-
                      lines number is reached.
                      Whether to stop to save log messages in local buffer after the specified memory-lines number is reached.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of an action.
                      Name of an action.
                    type: string
                  remember:
                    description: |-
                      (Boolean) Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                      Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                    type: boolean
                  remote:
                    description: |-
                      (String) Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                      Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                    type: string
                  remoteLogFormat:
                    description: |-
                      (String) Format for logs to be sent to remote instance:
                      Format for logs to be sent to remote instance:

                      - **cef** - logs are sent in CEF format;
                      - **default** - logs are sent as it is;
                      - **syslog** - logs are sent in BSD-syslog format.
                    type: string
                  remotePort:
                    description: |-
                      (Number) Remote logging server's UDP port, applicable if `action=remote`.
                      Remote logging server's UDP port, applicable if `action=remote`.
                    format: int64
                    type: integer
                  remoteProtocol:
                    description: |-
                      (String) Protocol for remote logging messages.
                      Protocol for remote logging messages.
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source address used when sending packets to remote server, applicable if `action=remote`.
                      Source address used when sending packets to remote server, applicable if `action=remote`.
                    type: string
                  syslogFacility:
                    description: |-
                      (String) SYSLOG facility, applicable if `action=remote`.
                      SYSLOG facility, applicable if `action=remote`.
                    type: string
                  syslogSeverity:
                    description: |-
                      (String) Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                      Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                    type: string
                  syslogTimeFormat:
                    description: |-
                      syslog` or `iso8601`).
                      SYSLOG time format (`bsd-syslog` or `iso8601`).
                    type: string
                  target:
                    description: |-
                      (String) Storage facility or target of log messages.
                      Storage facility or target of log messages.
                    type: string
                  vrf:
                    description: |-
                      (String) The VRF table this resource operates on.
                      The VRF table this resource operates on.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  bsdSyslog:
                    description: |-
                      syslog as defined in RFC 3164.
                      Whether to use bsd-syslog as defined in RFC 3164.
                    type: boolean
                  cefEventDelimiter:
                    description: |-
                      (String) Option helps remote syslog to distinguish between individual events within sent batch
                      Option helps remote syslog to distinguish between individual events within sent batch
                    type: string
                  diskFileCount:
                    description: |-
                      (Number) Specifies number of files used to store log messages, applicable only if `action=disk`.
                      Specifies number of files used to store log messages, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskFileName:
                    description: |-
                      (String) Name of the file used to store log messages, applicable only if `action=disk`.
                      Name of the file used to store log messages, applicable only if `action=disk`.
                    type: string
                  diskLinesPerFile:
                    description: |-
                      (Number) Specifies maximum size of file in lines, applicable only if `action=disk`.
                      Specifies maximum size of file in lines, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskStopOnFull:
                    description: |-
                      lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                      Whether to stop to save log messages to disk after the specified disk-lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                    type: boolean
                  emailStartTls:
                    description: |-
                      (Boolean) Whether to use tls when sending email, applicable only if `action=email`.
                      Whether to use tls when sending email, applicable only if `action=email`.
                    type: boolean
                  emailTo:
                    description: |-
                      (String) Email address where logs are sent, applicable only if `action=email`.
                      Email address where logs are sent, applicable only if `action=email`.
                    type: string
                  memoryLines:
                    description: |-
                      (Number) Number of records in local memory buffer, applicable only if `action=memory`.
                      Number of records in local memory buffer, applicable only if `action=memory`.
                    format: int64
                    type: integer
                  memoryStopOnFull:
                    description: |-
                      lines number is reached.
                      Whether to stop to save log messages in local buffer after the specified memory-lines number is reached.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of an action.
                      Name of an action.
                    type: string
                  remember:
                    description: |-
                      (Boolean) Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                      Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                    type: boolean
                  remote:
                    description: |-
                      (String) Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                      Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                    type: string
                  remoteLogFormat:
                    description: |-
                      (String) Format for logs to be sent to remote instance:
                      Format for logs to be sent to remote instance:

                      - **cef** - logs are sent in CEF format;
                      - **default** - logs are sent as it is;
                      - **syslog** - logs are sent in BSD-syslog format.
                    type: string
                  remotePort:
                    description: |-
                      (Number) Remote logging server's UDP port, applicable if `action=remote`.
                      Remote logging server's UDP port, applicable if `action=remote`.
                    format: int64
                    type: integer
                  remoteProtocol:
                    description: |-
                      (String) Protocol for remote logging messages.
                      Protocol for remote logging messages.
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source address used when sending packets to remote server, applicable if `action=remote`.
                      Source address used when sending packets to remote server, applicable if `action=remote`.
                    type: string
                  syslogFacility:
                    description: |-
                      (String) SYSLOG facility, applicable if `action=remote`.
                      SYSLOG facility, applicable if `action=remote`.
                    type: string
                  syslogSeverity:
                    description: |-
                      (String) Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                      Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                    type: string
                  syslogTimeFormat:
                    description: |-
                      syslog` or `iso8601`).
                      SYSLOG time format (`bsd-syslog` or `iso8601`).
                    type: string
                  target:
                    description: |-
                      (String) Storage facility or target of log messages.
                      Storage facility or target of log messages.
                    type: string
                  vrf:
                    description: |-
                      (String) The VRF table this resource operates on.
                      The VRF table this resource operates on.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.target is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.target)
                || (has(self.initProvider) && has(self.initProvider.target))'
          status:
            description: LoggingActionStatus defines the observed state of LoggingAction.
            properties:
              atProvider:
                properties:
                  bsdSyslog:
                    description: |-
                      syslog as defined in RFC 3164.
                      Whether to use bsd-syslog as defined in RFC 3164.
                    type: boolean
                  cefEventDelimiter:
                    description: |-
                      (String) Option helps remote syslog to distinguish between individual events within sent batch
                      Option helps remote syslog to distinguish between individual events within sent batch
                    type: string
                  default:
                    description: |-
                      (Boolean) It's the default item.
                      It's the default item.
                    type: boolean
                  diskFileCount:
                    description: |-
                      (Number) Specifies number of files used to store log messages, applicable only if `action=disk`.
                      Specifies number of files used to store log messages, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskFileName:
                    description: |-
                      (String) Name of the file used to store log messages, applicable only if `action=disk`.
                      Name of the file used to store log messages, applicable only if `action=disk`.
                    type: string
                  diskLinesPerFile:
                    description: |-
                      (Number) Specifies maximum size of file in lines, applicable only if `action=disk`.
                      Specifies maximum size of file in lines, applicable only if `action=disk`.
                    format: int64
                    type: integer
                  diskStopOnFull:
                    description: |-
                      lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                      Whether to stop to save log messages to disk after the specified disk-lines-per-file and disk-file-count number is reached, applicable only if `action=disk`.
                    type: boolean
                  emailStartTls:
                    description: |-
                      (Boolean) Whether to use tls when sending email, applicable only if `action=email`.
                      Whether to use tls when sending email, applicable only if `action=email`.
                    type: boolean
                  emailTo:
                    description: |-
                      (String) Email address where logs are sent, applicable only if `action=email`.
                      Email address where logs are sent, applicable only if `action=email`.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  memoryLines:
                    description: |-
                      (Number) Number of records in local memory buffer, applicable only if `action=memory`.
                      Number of records in local memory buffer, applicable only if `action=memory`.
                    format: int64
                    type: integer
                  memoryStopOnFull:
                    description: |-
                      lines number is reached.
                      Whether to stop to save log messages in local buffer after the specified memory-lines number is reached.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of an action.
                      Name of an action.
                    type: string
                  remember:
                    description: |-
                      (Boolean) Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                      Whether to keep log messages, which have not yet been displayed in console, applicable if `action=echo`.
                    type: boolean
                  remote:
                    description: |-
                      (String) Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                      Remote logging server's IP/IPv6 address, applicable if `action=remote`.
                    type: string
                  remoteLogFormat:
                    description: |-
                      (String) Format for logs to be sent to remote instance:
                      Format for logs to be sent to remote instance:

                      - **cef** - logs are sent in CEF format;
                      - **default** - logs are sent as it is;
                      - **syslog** - logs are sent in BSD-syslog format.
                    type: string
                  remotePort:
                    description: |-
                      (Number) Remote logging server's UDP port, applicable if `action=remote`.
                      Remote logging server's UDP port, applicable if `action=remote`.
                    format: int64
                    type: integer
                  remoteProtocol:
                    description: |-
                      (String) Protocol for remote logging messages.
                      Protocol for remote logging messages.
                    type: string
                  srcAddress:
                    description: |-
                      (String) Source address used when sending packets to remote server, applicable if `action=remote`.
                      Source address used when sending packets to remote server, applicable if `action=remote`.
                    type: string
                  syslogFacility:
                    description: |-
                      (String) SYSLOG facility, applicable if `action=remote`.
                      SYSLOG facility, applicable if `action=remote`.
                    type: string
                  syslogSeverity:
                    description: |-
                      (String) Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                      Severity level indicator defined in RFC 3164, applicable if `action=remote`.
                    type: string
                  syslogTimeFormat:
                    description: |-
                      syslog` or `iso8601`).
                      SYSLOG time format (`bsd-syslog` or `iso8601`).
                    type: string
                  target:
                    description: |-
                      (String) Storage facility or target of log messages.
                      Storage facility or target of log messages.
                    type: string
                  vrf:
                    description: |-
                      (String) The VRF table this resource operates on.
                      The VRF table this resource operates on.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
