---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: certificates.system.routeros.sindrip.io
spec:
  group: system.routeros.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: Certificate
    listKind: CertificateList
    plural: certificates
    singular: certificate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Certificate is the Schema for the Certificates API. Certificate
          resource management consists of two independent processes: * key creation
          and certificate signing request (`key` + `csr`) * certificate signing by
          the issuer (`crt`) For a complete certificate creation cycle, both of the
          above steps must be performed. In this case the `sign {}` block must be
          specified in the configuration. If you need to import the current state
          of the certificate resource, then do not specify the `sign{}` block. Importing
          an external certificate is also done without specifying the `sign{}` block,
          because the certificate should have already been signed by the issuer at
          this step.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateSpec defines the desired state of Certificate
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  commonName:
                    description: |-
                      (String) Common Name (e.g. server FQDN or YOUR name).
                      Common Name (e.g. server FQDN or YOUR name).
                    type: string
                  copyFrom:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) Country Name (2 letter code).
                      Country Name (2 letter code).
                    type: string
                  daysValid:
                    description: |-
                      (Number) Certificate lifetime.
                      Certificate lifetime.
                    format: int64
                    type: integer
                  import:
                    description: -import))
                    items:
                      properties:
                        certFileContent:
                          description: |-
                            (String) Certificate in PEM format.
                            Certificate in PEM format.
                          type: string
                        certFileName:
                          description: |-
                            (String) Certificate file name that will be imported.
                            Certificate file name that will be imported.
                          type: string
                        keyFileContent:
                          description: |-
                            (String) Key in PEM format.
                            Key in PEM format.
                          type: string
                        keyFileName:
                          description: |-
                            (String) Key file name that will be imported.
                            Key file name that will be imported.
                          type: string
                        passphraseSecretRef:
                          description: |-
                            (String, Sensitive) File passphrase if there is such.
                            File passphrase if there is such.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  keySize:
                    description: (String)
                    type: string
                  keyUsage:
                    description: |-
                      (Set of String) Detailed key usage descriptions can be found in RFC 5280.
                      Detailed key usage descriptions can be found in RFC 5280.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  locality:
                    description: |-
                      (String) Locality Name (eg, city).
                      Locality Name (eg, city).
                    type: string
                  name:
                    description: |-
                      (String) Name of the certificate. Name can be edited.
                      Name of the certificate. Name can be edited.
                    type: string
                  organization:
                    description: |-
                      (String) Organizational Unit Name (eg, section)
                      Organizational Unit Name (eg, section)
                    type: string
                  sign:
                    description: -sign))
                    items:
                      properties:
                        ca:
                          description: |-
                            (String) Which CA to use if signing issued certificates.
                            Which CA to use if signing issued certificates.
                          type: string
                        caCrlHost:
                          description: |-
                            (String) CRL host if issuing CA certificate.
                            CRL host if issuing CA certificate.
                          type: string
                      type: object
                    type: array
                  signViaScep:
                    description: -sign_via_scep))
                    items:
                      properties:
                        caIdentity:
                          description: |-
                            (String) SCEP CA identity.
                            SCEP CA identity.
                          type: string
                        challengePasswordSecretRef:
                          description: |-
                            (String, Sensitive) A challenge password.
                            A challenge password.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        onSmartCard:
                          description: |-
                            (Boolean) Whether to store a private key on smart card if hardware supports it.
                            Whether to store a private key on smart card if hardware supports it.
                          type: boolean
                        refresh:
                          description: |-
                            (Boolean) Check certificate expiration and refresh it if expired.
                            Check certificate expiration and refresh it if expired.
                          type: boolean
                        scepUrl:
                          description: |-
                            (String) HTTP URL to the SCEP server.
                            HTTP URL to the SCEP server.
                          type: string
                      type: object
                    type: array
                  state:
                    description: |-
                      (String) State or Province Name (full name).
                      State or Province Name (full name).
                    type: string
                  subjectAltName:
                    description: |-
                      (String) SANs (subject alternative names).
                      SANs (subject alternative names).
                    type: string
                  trusted:
                    description: |-
                      (Boolean) If set to yes certificate is included 'in trusted certificate chain'.
                      If set to yes certificate is included 'in trusted certificate chain'.
                    type: boolean
                  unit:
                    description: |-
                      (String) Organizational Unit Name (eg, section).
                      Organizational Unit Name (eg, section).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  commonName:
                    description: |-
                      (String) Common Name (e.g. server FQDN or YOUR name).
                      Common Name (e.g. server FQDN or YOUR name).
                    type: string
                  copyFrom:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) Country Name (2 letter code).
                      Country Name (2 letter code).
                    type: string
                  daysValid:
                    description: |-
                      (Number) Certificate lifetime.
                      Certificate lifetime.
                    format: int64
                    type: integer
                  import:
                    description: -import))
                    items:
                      properties:
                        certFileContent:
                          description: |-
                            (String) Certificate in PEM format.
                            Certificate in PEM format.
                          type: string
                        certFileName:
                          description: |-
                            (String) Certificate file name that will be imported.
                            Certificate file name that will be imported.
                          type: string
                        keyFileContent:
                          description: |-
                            (String) Key in PEM format.
                            Key in PEM format.
                          type: string
                        keyFileName:
                          description: |-
                            (String) Key file name that will be imported.
                            Key file name that will be imported.
                          type: string
                        passphraseSecretRef:
                          description: |-
                            (String, Sensitive) File passphrase if there is such.
                            File passphrase if there is such.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  keySize:
                    description: (String)
                    type: string
                  keyUsage:
                    description: |-
                      (Set of String) Detailed key usage descriptions can be found in RFC 5280.
                      Detailed key usage descriptions can be found in RFC 5280.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  locality:
                    description: |-
                      (String) Locality Name (eg, city).
                      Locality Name (eg, city).
                    type: string
                  name:
                    description: |-
                      (String) Name of the certificate. Name can be edited.
                      Name of the certificate. Name can be edited.
                    type: string
                  organization:
                    description: |-
                      (String) Organizational Unit Name (eg, section)
                      Organizational Unit Name (eg, section)
                    type: string
                  sign:
                    description: -sign))
                    items:
                      properties:
                        ca:
                          description: |-
                            (String) Which CA to use if signing issued certificates.
                            Which CA to use if signing issued certificates.
                          type: string
                        caCrlHost:
                          description: |-
                            (String) CRL host if issuing CA certificate.
                            CRL host if issuing CA certificate.
                          type: string
                      type: object
                    type: array
                  signViaScep:
                    description: -sign_via_scep))
                    items:
                      properties:
                        caIdentity:
                          description: |-
                            (String) SCEP CA identity.
                            SCEP CA identity.
                          type: string
                        challengePasswordSecretRef:
                          description: |-
                            (String, Sensitive) A challenge password.
                            A challenge password.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        onSmartCard:
                          description: |-
                            (Boolean) Whether to store a private key on smart card if hardware supports it.
                            Whether to store a private key on smart card if hardware supports it.
                          type: boolean
                        refresh:
                          description: |-
                            (Boolean) Check certificate expiration and refresh it if expired.
                            Check certificate expiration and refresh it if expired.
                          type: boolean
                        scepUrl:
                          description: |-
                            (String) HTTP URL to the SCEP server.
                            HTTP URL to the SCEP server.
                          type: string
                      type: object
                    type: array
                  state:
                    description: |-
                      (String) State or Province Name (full name).
                      State or Province Name (full name).
                    type: string
                  subjectAltName:
                    description: |-
                      (String) SANs (subject alternative names).
                      SANs (subject alternative names).
                    type: string
                  trusted:
                    description: |-
                      (Boolean) If set to yes certificate is included 'in trusted certificate chain'.
                      If set to yes certificate is included 'in trusted certificate chain'.
                    type: boolean
                  unit:
                    description: |-
                      (String) Organizational Unit Name (eg, section).
                      Organizational Unit Name (eg, section).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.commonName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.commonName)
                || (has(self.initProvider) && has(self.initProvider.commonName))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: CertificateStatus defines the observed state of Certificate.
            properties:
              atProvider:
                properties:
                  akid:
                    description: |-
                      (String) Authority Key Identifier.
                      Authority Key Identifier.
                    type: string
                  authority:
                    description: (String)
                    type: string
                  ca:
                    description: (String) Which CA to use if signing issued certificates.
                    type: string
                  caCrlHost:
                    description: (String) CRL host if issuing CA certificate.
                    type: string
                  caFingerprint:
                    description: (String)
                    type: string
                  commonName:
                    description: |-
                      (String) Common Name (e.g. server FQDN or YOUR name).
                      Common Name (e.g. server FQDN or YOUR name).
                    type: string
                  copyFrom:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) Country Name (2 letter code).
                      Country Name (2 letter code).
                    type: string
                  crl:
                    description: (String)
                    type: string
                  daysValid:
                    description: |-
                      (Number) Certificate lifetime.
                      Certificate lifetime.
                    format: int64
                    type: integer
                  digestAlgorithm:
                    description: |-
                      (Boolean)
                      RouterOS accepts "md5", "sha1", "sha256", "sha384" or "sha512"; it rejects true, false, yes and no.
                    type: string
                  dsa:
                    description: (Boolean)
                    type: boolean
                  expired:
                    description: |-
                      (Boolean) Set to true if certificate is expired.
                      Set to true if certificate is expired.
                    type: boolean
                  expiresAfter:
                    description: (String)
                    type: string
                  fingerprint:
                    description: (String)
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  import:
                    description: -import))
                    items:
                      properties:
                        certFileContent:
                          description: |-
                            (String) Certificate in PEM format.
                            Certificate in PEM format.
                          type: string
                        certFileName:
                          description: |-
                            (String) Certificate file name that will be imported.
                            Certificate file name that will be imported.
                          type: string
                        keyFileContent:
                          description: |-
                            (String) Key in PEM format.
                            Key in PEM format.
                          type: string
                        keyFileName:
                          description: |-
                            (String) Key file name that will be imported.
                            Key file name that will be imported.
                          type: string
                      type: object
                    type: array
                  invalidAfter:
                    description: |-
                      (String) The date after which certificate wil be invalid.
                      The date after which certificate wil be invalid.
                    type: string
                  invalidBefore:
                    description: |-
                      (String) The date before which certificate is invalid.
                      The date before which certificate is invalid.
                    type: string
                  issued:
                    description: (String)
                    type: string
                  issuer:
                    description: (String)
                    type: string
                  keySize:
                    description: (String)
                    type: string
                  keyType:
                    description: (String)
                    type: string
                  keyUsage:
                    description: |-
                      (Set of String) Detailed key usage descriptions can be found in RFC 5280.
                      Detailed key usage descriptions can be found in RFC 5280.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  locality:
                    description: |-
                      (String) Locality Name (eg, city).
                      Locality Name (eg, city).
                    type: string
                  name:
                    description: |-
                      (String) Name of the certificate. Name can be edited.
                      Name of the certificate. Name can be edited.
                    type: string
                  organization:
                    description: |-
                      (String) Organizational Unit Name (eg, section)
                      Organizational Unit Name (eg, section)
                    type: string
                  privateKey:
                    description: (Boolean)
                    type: boolean
                  reqFingerprint:
                    description: (String)
                    type: string
                  revoked:
                    description: (String)
                    type: string
                  scepUrl:
                    description: (String) HTTP URL to the SCEP server.
                    type: string
                  serialNumber:
                    description: (String)
                    type: string
                  sign:
                    description: -sign))
                    items:
                      properties:
                        ca:
                          description: |-
                            (String) Which CA to use if signing issued certificates.
                            Which CA to use if signing issued certificates.
                          type: string
                        caCrlHost:
                          description: |-
                            (String) CRL host if issuing CA certificate.
                            CRL host if issuing CA certificate.
                          type: string
                      type: object
                    type: array
                  signViaScep:
                    description: -sign_via_scep))
                    items:
                      properties:
                        caIdentity:
                          description: |-
                            (String) SCEP CA identity.
                            SCEP CA identity.
                          type: string
                        onSmartCard:
                          description: |-
                            (Boolean) Whether to store a private key on smart card if hardware supports it.
                            Whether to store a private key on smart card if hardware supports it.
                          type: boolean
                        refresh:
                          description: |-
                            (Boolean) Check certificate expiration and refresh it if expired.
                            Check certificate expiration and refresh it if expired.
                          type: boolean
                        scepUrl:
                          description: |-
                            (String) HTTP URL to the SCEP server.
                            HTTP URL to the SCEP server.
                          type: string
                      type: object
                    type: array
                  skid:
                    description: |-
                      (String) Subject Key Identifier.
                      Subject Key Identifier.
                    type: string
                  smartCardKey:
                    description: (String)
                    type: string
                  state:
                    description: |-
                      (String) State or Province Name (full name).
                      State or Province Name (full name).
                    type: string
                  status:
                    description: |-
                      (String) Shows current status of scep client.
                      Shows current status of scep client.
                    type: string
                  subjectAltName:
                    description: |-
                      (String) SANs (subject alternative names).
                      SANs (subject alternative names).
                    type: string
                  trusted:
                    description: |-
                      (Boolean) If set to yes certificate is included 'in trusted certificate chain'.
                      If set to yes certificate is included 'in trusted certificate chain'.
                    type: boolean
                  unit:
                    description: |-
                      (String) Organizational Unit Name (eg, section).
                      Organizational Unit Name (eg, section).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
