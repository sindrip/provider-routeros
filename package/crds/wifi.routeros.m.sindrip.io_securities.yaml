---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: securities.wifi.routeros.m.sindrip.io
spec:
  group: wifi.routeros.m.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: Security
    listKind: SecurityList
    plural: securities
    singular: security
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Security is the Schema for the Securitys API. *<span style="color:red">This
          resource requires a minimum version of RouterOS 7.13.</span>*
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecuritySpec defines the desired state of Security
            properties:
              forProvider:
                properties:
                  authenticationTypes:
                    description: |-
                      (Set of String) Authentication types to enable on the interface.
                      Authentication types to enable on the interface.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  connectGroup:
                    description: |-
                      (String) APs within the same connect group do not allow more than 1 client device with the same MAC address.
                      APs within the same connect group do not allow more than 1 client device with the same MAC address.
                    type: string
                  connectPriority:
                    description: |-
                      (String) An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                      An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                    type: string
                  dhGroups:
                    description: |-
                      (Set of Number) Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                      Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  disablePmkid:
                    description: |-
                      (Boolean) An option to disable inclusion of a PMKID in EAPOL frames.
                      An option to disable inclusion of a PMKID in EAPOL frames.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  eapAccounting:
                    description: |-
                      authenticated peers.
                      An option to send accounting information to RADIUS server for EAP-authenticated peers.
                    type: boolean
                  eapAnonymousIdentity:
                    description: |-
                      (String) An option to specify anonymous identity for EAP outer authentication.
                      An option to specify anonymous identity for EAP outer authentication.
                    type: string
                  eapCertificateMode:
                    description: |-
                      (String) A policy for handling the TLS certificate of the RADIUS server.
                      A policy for handling the TLS certificate of the RADIUS server.
                    type: string
                  eapMethods:
                    description: |-
                      (Set of String) A set of EAP methods to consider for authentication.
                      A set of EAP methods to consider for authentication.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  eapPassword:
                    description: |-
                      (String) Password to use when the chosen EAP method requires one.
                      Password to use when the chosen EAP method requires one.
                    type: string
                  eapTlsCertificate:
                    description: |-
                      (String) Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                      Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                    type: string
                  eapUsername:
                    description: |-
                      (String) Username to use when the chosen EAP method requires one.
                      Username to use when the chosen EAP method requires one.
                    type: string
                  encryption:
                    description: |-
                      (Set of String) A list of ciphers to support for encrypting unicast traffic.
                      A list of ciphers to support for encrypting unicast traffic.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ft:
                    description: |-
                      (Boolean) An option to enable 802.11r fast BSS transitions (roaming).
                      An option to enable 802.11r fast BSS transitions (roaming).
                    type: boolean
                  ftMobilityDomain:
                    description: |-
                      (String) The fast BSS transition mobility domain ID.
                      The fast BSS transition mobility domain ID.
                    type: string
                  ftNasIdentifier:
                    description: |-
                      R0 key holder identifier.
                      Fast BSS transition PMK-R0 key holder identifier.
                    type: string
                  ftOverDs:
                    description: |-
                      (Boolean) An option to enable fast BSS transitions over DS (distributed system).
                      An option to enable fast BSS transitions over DS (distributed system).
                    type: boolean
                  ftPreserveVlanid:
                    description: |-
                      (Boolean) An option to preserve VLAN ID when roaming.
                      An option to preserve VLAN ID when roaming.
                    type: boolean
                  ftR0KeyLifetime:
                    description: |-
                      R0 encryption key.
                      The lifetime of the fast BSS transition PMK-R0 encryption key.
                    type: string
                  ftReassociationDeadline:
                    description: |-
                      (String) Fast BSS transition reassociation deadline.
                      Fast BSS transition reassociation deadline.
                    type: string
                  groupEncryption:
                    description: |-
                      (String) A cipher to use for encrypting multicast traffic.
                      A cipher to use for encrypting multicast traffic.
                    type: string
                  groupKeyUpdate:
                    description: |-
                      (String) The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                      The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                    type: string
                  managementEncryption:
                    description: |-
                      (String) A cipher to use for encrypting protected management frames.
                      A cipher to use for encrypting protected management frames.
                    type: string
                  managementProtection:
                    description: |-
                      (String) An option to enable 802.11w management frame protection.
                      An option to enable 802.11w management frame protection.
                    type: string
                  multiPassphraseGroup:
                    description: |-
                      passphrase/` group that will be used. Only a single group can be defined under the security profile.
                      Name of `/interface/wifi/security/multi-passphrase/` group that will be used. Only a single group can be defined under the security profile.
                    type: string
                  name:
                    description: |-
                      (String) Name of the security profile.
                      Name of the security profile.
                    type: string
                  oweTransitionInterface:
                    description: |-
                      (String) Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                      Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                    type: string
                  passphrase:
                    description: |-
                      (String) Passphrase to use for PSK authentication types.
                      Passphrase to use for PSK authentication types.
                    type: string
                  saeAntiCloggingThreshold:
                    description: |-
                      progress SAE authentications.
                      A parameter to mitigate DoS attacks by specifying a threshold of in-progress SAE authentications.
                    type: string
                  saeMaxFailureRate:
                    description: |-
                      (String) Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                      Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                    type: string
                  saePwe:
                    description: |-
                      (String) Methods to support for deriving SAE password element.
                      Methods to support for deriving SAE password element.
                    type: string
                  wps:
                    description: |-
                      Fi Protected Setup).
                      An option to enable WPS (Wi-Fi Protected Setup).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationTypes:
                    description: |-
                      (Set of String) Authentication types to enable on the interface.
                      Authentication types to enable on the interface.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  connectGroup:
                    description: |-
                      (String) APs within the same connect group do not allow more than 1 client device with the same MAC address.
                      APs within the same connect group do not allow more than 1 client device with the same MAC address.
                    type: string
                  connectPriority:
                    description: |-
                      (String) An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                      An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                    type: string
                  dhGroups:
                    description: |-
                      (Set of Number) Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                      Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  disablePmkid:
                    description: |-
                      (Boolean) An option to disable inclusion of a PMKID in EAPOL frames.
                      An option to disable inclusion of a PMKID in EAPOL frames.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  eapAccounting:
                    description: |-
                      authenticated peers.
                      An option to send accounting information to RADIUS server for EAP-authenticated peers.
                    type: boolean
                  eapAnonymousIdentity:
                    description: |-
                      (String) An option to specify anonymous identity for EAP outer authentication.
                      An option to specify anonymous identity for EAP outer authentication.
                    type: string
                  eapCertificateMode:
                    description: |-
                      (String) A policy for handling the TLS certificate of the RADIUS server.
                      A policy for handling the TLS certificate of the RADIUS server.
                    type: string
                  eapMethods:
                    description: |-
                      (Set of String) A set of EAP methods to consider for authentication.
                      A set of EAP methods to consider for authentication.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  eapPassword:
                    description: |-
                      (String) Password to use when the chosen EAP method requires one.
                      Password to use when the chosen EAP method requires one.
                    type: string
                  eapTlsCertificate:
                    description: |-
                      (String) Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                      Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                    type: string
                  eapUsername:
                    description: |-
                      (String) Username to use when the chosen EAP method requires one.
                      Username to use when the chosen EAP method requires one.
                    type: string
                  encryption:
                    description: |-
                      (Set of String) A list of ciphers to support for encrypting unicast traffic.
                      A list of ciphers to support for encrypting unicast traffic.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ft:
                    description: |-
                      (Boolean) An option to enable 802.11r fast BSS transitions (roaming).
                      An option to enable 802.11r fast BSS transitions (roaming).
                    type: boolean
                  ftMobilityDomain:
                    description: |-
                      (String) The fast BSS transition mobility domain ID.
                      The fast BSS transition mobility domain ID.
                    type: string
                  ftNasIdentifier:
                    description: |-
                      R0 key holder identifier.
                      Fast BSS transition PMK-R0 key holder identifier.
                    type: string
                  ftOverDs:
                    description: |-
                      (Boolean) An option to enable fast BSS transitions over DS (distributed system).
                      An option to enable fast BSS transitions over DS (distributed system).
                    type: boolean
                  ftPreserveVlanid:
                    description: |-
                      (Boolean) An option to preserve VLAN ID when roaming.
                      An option to preserve VLAN ID when roaming.
                    type: boolean
                  ftR0KeyLifetime:
                    description: |-
                      R0 encryption key.
                      The lifetime of the fast BSS transition PMK-R0 encryption key.
                    type: string
                  ftReassociationDeadline:
                    description: |-
                      (String) Fast BSS transition reassociation deadline.
                      Fast BSS transition reassociation deadline.
                    type: string
                  groupEncryption:
                    description: |-
                      (String) A cipher to use for encrypting multicast traffic.
                      A cipher to use for encrypting multicast traffic.
                    type: string
                  groupKeyUpdate:
                    description: |-
                      (String) The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                      The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                    type: string
                  managementEncryption:
                    description: |-
                      (String) A cipher to use for encrypting protected management frames.
                      A cipher to use for encrypting protected management frames.
                    type: string
                  managementProtection:
                    description: |-
                      (String) An option to enable 802.11w management frame protection.
                      An option to enable 802.11w management frame protection.
                    type: string
                  multiPassphraseGroup:
                    description: |-
                      passphrase/` group that will be used. Only a single group can be defined under the security profile.
                      Name of `/interface/wifi/security/multi-passphrase/` group that will be used. Only a single group can be defined under the security profile.
                    type: string
                  name:
                    description: |-
                      (String) Name of the security profile.
                      Name of the security profile.
                    type: string
                  oweTransitionInterface:
                    description: |-
                      (String) Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                      Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                    type: string
                  passphrase:
                    description: |-
                      (String) Passphrase to use for PSK authentication types.
                      Passphrase to use for PSK authentication types.
                    type: string
                  saeAntiCloggingThreshold:
                    description: |-
                      progress SAE authentications.
                      A parameter to mitigate DoS attacks by specifying a threshold of in-progress SAE authentications.
                    type: string
                  saeMaxFailureRate:
                    description: |-
                      (String) Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                      Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                    type: string
                  saePwe:
                    description: |-
                      (String) Methods to support for deriving SAE password element.
                      Methods to support for deriving SAE password element.
                    type: string
                  wps:
                    description: |-
                      Fi Protected Setup).
                      An option to enable WPS (Wi-Fi Protected Setup).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SecurityStatus defines the observed state of Security.
            properties:
              atProvider:
                properties:
                  authenticationTypes:
                    description: |-
                      (Set of String) Authentication types to enable on the interface.
                      Authentication types to enable on the interface.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: (String)
                    type: string
                  connectGroup:
                    description: |-
                      (String) APs within the same connect group do not allow more than 1 client device with the same MAC address.
                      APs within the same connect group do not allow more than 1 client device with the same MAC address.
                    type: string
                  connectPriority:
                    description: |-
                      (String) An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                      An option to determine how a connection is handled if the MAC address of the client device is the same as that of another active connection to another AP.
                    type: string
                  dhGroups:
                    description: |-
                      (Set of Number) Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                      Identifiers of elliptic curve cryptography groups to use in SAE (WPA3) authentication.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  disablePmkid:
                    description: |-
                      (Boolean) An option to disable inclusion of a PMKID in EAPOL frames.
                      An option to disable inclusion of a PMKID in EAPOL frames.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  eapAccounting:
                    description: |-
                      authenticated peers.
                      An option to send accounting information to RADIUS server for EAP-authenticated peers.
                    type: boolean
                  eapAnonymousIdentity:
                    description: |-
                      (String) An option to specify anonymous identity for EAP outer authentication.
                      An option to specify anonymous identity for EAP outer authentication.
                    type: string
                  eapCertificateMode:
                    description: |-
                      (String) A policy for handling the TLS certificate of the RADIUS server.
                      A policy for handling the TLS certificate of the RADIUS server.
                    type: string
                  eapMethods:
                    description: |-
                      (Set of String) A set of EAP methods to consider for authentication.
                      A set of EAP methods to consider for authentication.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  eapPassword:
                    description: |-
                      (String) Password to use when the chosen EAP method requires one.
                      Password to use when the chosen EAP method requires one.
                    type: string
                  eapTlsCertificate:
                    description: |-
                      (String) Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                      Name or id of a certificate in the device's certificate store to use when the chosen EAP authentication method requires one.
                    type: string
                  eapUsername:
                    description: |-
                      (String) Username to use when the chosen EAP method requires one.
                      Username to use when the chosen EAP method requires one.
                    type: string
                  encryption:
                    description: |-
                      (Set of String) A list of ciphers to support for encrypting unicast traffic.
                      A list of ciphers to support for encrypting unicast traffic.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ft:
                    description: |-
                      (Boolean) An option to enable 802.11r fast BSS transitions (roaming).
                      An option to enable 802.11r fast BSS transitions (roaming).
                    type: boolean
                  ftMobilityDomain:
                    description: |-
                      (String) The fast BSS transition mobility domain ID.
                      The fast BSS transition mobility domain ID.
                    type: string
                  ftNasIdentifier:
                    description: |-
                      R0 key holder identifier.
                      Fast BSS transition PMK-R0 key holder identifier.
                    type: string
                  ftOverDs:
                    description: |-
                      (Boolean) An option to enable fast BSS transitions over DS (distributed system).
                      An option to enable fast BSS transitions over DS (distributed system).
                    type: boolean
                  ftPreserveVlanid:
                    description: |-
                      (Boolean) An option to preserve VLAN ID when roaming.
                      An option to preserve VLAN ID when roaming.
                    type: boolean
                  ftR0KeyLifetime:
                    description: |-
                      R0 encryption key.
                      The lifetime of the fast BSS transition PMK-R0 encryption key.
                    type: string
                  ftReassociationDeadline:
                    description: |-
                      (String) Fast BSS transition reassociation deadline.
                      Fast BSS transition reassociation deadline.
                    type: string
                  groupEncryption:
                    description: |-
                      (String) A cipher to use for encrypting multicast traffic.
                      A cipher to use for encrypting multicast traffic.
                    type: string
                  groupKeyUpdate:
                    description: |-
                      (String) The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                      The interval at which the group temporal key (key for encrypting broadcast traffic) is renewed.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  managementEncryption:
                    description: |-
                      (String) A cipher to use for encrypting protected management frames.
                      A cipher to use for encrypting protected management frames.
                    type: string
                  managementProtection:
                    description: |-
                      (String) An option to enable 802.11w management frame protection.
                      An option to enable 802.11w management frame protection.
                    type: string
                  multiPassphraseGroup:
                    description: |-
                      passphrase/` group that will be used. Only a single group can be defined under the security profile.
                      Name of `/interface/wifi/security/multi-passphrase/` group that will be used. Only a single group can be defined under the security profile.
                    type: string
                  name:
                    description: |-
                      (String) Name of the security profile.
                      Name of the security profile.
                    type: string
                  oweTransitionInterface:
                    description: |-
                      (String) Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                      Name or internal ID of an interface which MAC address and SSID to advertise as the matching AP when running in OWE transition mode.
                    type: string
                  passphrase:
                    description: |-
                      (String) Passphrase to use for PSK authentication types.
                      Passphrase to use for PSK authentication types.
                    type: string
                  saeAntiCloggingThreshold:
                    description: |-
                      progress SAE authentications.
                      A parameter to mitigate DoS attacks by specifying a threshold of in-progress SAE authentications.
                    type: string
                  saeMaxFailureRate:
                    description: |-
                      (String) Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                      Rate of failed SAE (WPA3) associations per minute, at which the AP will stop processing new association requests.
                    type: string
                  saePwe:
                    description: |-
                      (String) Methods to support for deriving SAE password element.
                      Methods to support for deriving SAE password element.
                    type: string
                  wps:
                    description: |-
                      Fi Protected Setup).
                      An option to enable WPS (Wi-Fi Protected Setup).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
