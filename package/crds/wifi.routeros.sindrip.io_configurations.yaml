---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: configurations.wifi.routeros.sindrip.io
spec:
  group: wifi.routeros.sindrip.io
  names:
    categories:
    - crossplane
    - managed
    - routeros
    kind: Configuration
    listKind: ConfigurationList
    plural: configurations
    singular: configuration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Configuration is the Schema for the Configurations API. *<span
          style="color:red">This resource requires a minimum version of RouterOS 7.13.</span>*
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigurationSpec defines the desired state of Configuration
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  aaa:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) AAA inline settings.
                      AAA inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  antennaGain:
                    description: |-
                      (Number) An option overrides the default antenna gain.
                      An option overrides the default antenna gain.
                    format: int64
                    type: integer
                  beaconInterval:
                    description: |-
                      (String) Time interval between beacon frames.
                      Time interval between beacon frames.
                    type: string
                  chains:
                    description: |-
                      (Set of Number) Radio chains to use for receiving signals.
                      Radio chains to use for receiving signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  channel:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Channel inline settings.
                      Channel inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  comment:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) An option determines which regulatory domain restrictions are applied to an interface.
                      An option determines which regulatory domain restrictions are applied to an interface.
                    type: string
                  datapath:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Datapath inline settings.
                      Datapath inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  deprioritizeUnii34:
                    description: |-
                      5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                      Whether to assign lower priority to channels with a control frequency of 5720 or 5825-5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dtimPeriod:
                    description: |-
                      (Number) A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                      A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                    format: int64
                    type: integer
                  hideSsid:
                    description: |-
                      (Boolean) This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                      This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                    type: boolean
                  interworking:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Interworking inline settings.
                      Interworking inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  manager:
                    description: |-
                      (String) An option to specify the remote CAP mode.
                      An option to specify the remote CAP mode.
                    type: string
                  mode:
                    description: |-
                      (String) An option to specify the access point operational mode.
                      An option to specify the access point operational mode.
                    type: string
                  multicastEnhance:
                    description: |-
                      address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                      An option to enable converting every multicast-address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                    type: string
                  name:
                    description: |-
                      (String) Name of the configuration.
                      Name of the configuration.
                    type: string
                  qosClassifier:
                    description: |-
                      (String) An option to specify the QoS classifier.
                      An option to specify the QoS classifier.
                    type: string
                  security:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Security inline settings.
                      Security inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                      SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                    type: string
                  steering:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Steering inline settings.
                      Steering inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  txChains:
                    description: |-
                      (Set of Number) Radio chains to use for transmitting signals.
                      Radio chains to use for transmitting signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  txPower:
                    description: |-
                      (Number) A limit on the transmit power (in dBm) of the interface.
                      A limit on the transmit power (in dBm) of the interface.
                    format: int64
                    type: integer
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  aaa:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) AAA inline settings.
                      AAA inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  antennaGain:
                    description: |-
                      (Number) An option overrides the default antenna gain.
                      An option overrides the default antenna gain.
                    format: int64
                    type: integer
                  beaconInterval:
                    description: |-
                      (String) Time interval between beacon frames.
                      Time interval between beacon frames.
                    type: string
                  chains:
                    description: |-
                      (Set of Number) Radio chains to use for receiving signals.
                      Radio chains to use for receiving signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  channel:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Channel inline settings.
                      Channel inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  comment:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) An option determines which regulatory domain restrictions are applied to an interface.
                      An option determines which regulatory domain restrictions are applied to an interface.
                    type: string
                  datapath:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Datapath inline settings.
                      Datapath inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  deprioritizeUnii34:
                    description: |-
                      5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                      Whether to assign lower priority to channels with a control frequency of 5720 or 5825-5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dtimPeriod:
                    description: |-
                      (Number) A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                      A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                    format: int64
                    type: integer
                  hideSsid:
                    description: |-
                      (Boolean) This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                      This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                    type: boolean
                  interworking:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Interworking inline settings.
                      Interworking inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  manager:
                    description: |-
                      (String) An option to specify the remote CAP mode.
                      An option to specify the remote CAP mode.
                    type: string
                  mode:
                    description: |-
                      (String) An option to specify the access point operational mode.
                      An option to specify the access point operational mode.
                    type: string
                  multicastEnhance:
                    description: |-
                      address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                      An option to enable converting every multicast-address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                    type: string
                  name:
                    description: |-
                      (String) Name of the configuration.
                      Name of the configuration.
                    type: string
                  qosClassifier:
                    description: |-
                      (String) An option to specify the QoS classifier.
                      An option to specify the QoS classifier.
                    type: string
                  security:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Security inline settings.
                      Security inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                      SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                    type: string
                  steering:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Steering inline settings.
                      Steering inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  txChains:
                    description: |-
                      (Set of Number) Radio chains to use for transmitting signals.
                      Radio chains to use for transmitting signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  txPower:
                    description: |-
                      (Number) A limit on the transmit power (in dBm) of the interface.
                      A limit on the transmit power (in dBm) of the interface.
                    format: int64
                    type: integer
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ConfigurationStatus defines the observed state of Configuration.
            properties:
              atProvider:
                properties:
                  aaa:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) AAA inline settings.
                      AAA inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  antennaGain:
                    description: |-
                      (Number) An option overrides the default antenna gain.
                      An option overrides the default antenna gain.
                    format: int64
                    type: integer
                  beaconInterval:
                    description: |-
                      (String) Time interval between beacon frames.
                      Time interval between beacon frames.
                    type: string
                  chains:
                    description: |-
                      (Set of Number) Radio chains to use for receiving signals.
                      Radio chains to use for receiving signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  channel:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Channel inline settings.
                      Channel inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  comment:
                    description: (String)
                    type: string
                  country:
                    description: |-
                      (String) An option determines which regulatory domain restrictions are applied to an interface.
                      An option determines which regulatory domain restrictions are applied to an interface.
                    type: string
                  datapath:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Datapath inline settings.
                      Datapath inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  deprioritizeUnii34:
                    description: |-
                      5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                      Whether to assign lower priority to channels with a control frequency of 5720 or 5825-5885 MHz. These channels are unsupported by some client devices, making their automatic selection undesirable. Defaults to `yes` in ETSI regulatory domains, elsewhere to `no`.
                    type: boolean
                  disabled:
                    description: (Boolean)
                    type: boolean
                  dtimPeriod:
                    description: |-
                      (Number) A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                      A period at which to transmit multicast traffic, when there are client devices in power save mode connected to the AP.
                    format: int64
                    type: integer
                  hideSsid:
                    description: |-
                      (Boolean) This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                      This property has effect only in AP mode. Setting it to yes can remove this network from the list of wireless networks that are shown by some client software. Changing this setting does not improve the security of the wireless network, because SSID is included in other frames sent by the AP.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interworking:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Interworking inline settings.
                      Interworking inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  manager:
                    description: |-
                      (String) An option to specify the remote CAP mode.
                      An option to specify the remote CAP mode.
                    type: string
                  mode:
                    description: |-
                      (String) An option to specify the access point operational mode.
                      An option to specify the access point operational mode.
                    type: string
                  multicastEnhance:
                    description: |-
                      address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                      An option to enable converting every multicast-address IP or IPv6 packet into multiple unicast-addresses frames for each connected station.
                    type: string
                  name:
                    description: |-
                      (String) Name of the configuration.
                      Name of the configuration.
                    type: string
                  qosClassifier:
                    description: |-
                      (String) An option to specify the QoS classifier.
                      An option to specify the QoS classifier.
                    type: string
                  security:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Security inline settings.
                      Security inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  ssid:
                    description: |-
                      (String) SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                      SSID (service set identifier) is a name broadcast in the beacons that identifies wireless network.
                    type: string
                  steering:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Steering inline settings.
                      Steering inline settings.
                    type: object
                    x-kubernetes-map-type: granular
                  txChains:
                    description: |-
                      (Set of Number) Radio chains to use for transmitting signals.
                      Radio chains to use for transmitting signals.
                    items:
                      format: int64
                      type: integer
                    type: array
                    x-kubernetes-list-type: set
                  txPower:
                    description: |-
                      (Number) A limit on the transmit power (in dBm) of the interface.
                      A limit on the transmit power (in dBm) of the interface.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
